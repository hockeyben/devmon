---
phase: 07-party-and-collection
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/devmon/commands/collection.py
  - src/devmon/main.py
  - tests/test_collection.py
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - CLI-04
  - UI-05

must_haves:
  truths:
    - "devmon collection shows all captured creatures in a Rich table"
    - "Collection table is sortable by rarity, level, or name"
    - "Party members show [P] badge in collection list"
    - "devmon collection <name> shows full creature detail panel"
    - "devmon collection codex lists all 25 creatures with discovery state"
    - "Codex shows completeness counter and progress bar"
    - "devmon collection rename changes a creature's nickname"
    - "Renamed creatures display new name everywhere"
  artifacts:
    - path: "src/devmon/commands/collection.py"
      provides: "Collection list, detail, codex, and rename commands"
      exports: ["app"]
    - path: "tests/test_collection.py"
      provides: "Collection and codex test coverage"
  key_links:
    - from: "src/devmon/commands/collection.py"
      to: "src/devmon/engine/creature_loader.py"
      via: "load_all_creatures() for codex, get_creature() for detail"
      pattern: "load_all_creatures|get_creature"
    - from: "src/devmon/commands/collection.py"
      to: "src/devmon/render/creatures.py"
      via: "render_creature_panel() for detail view"
      pattern: "render_creature_panel"
    - from: "src/devmon/main.py"
      to: "src/devmon/commands/collection.py"
      via: "app.add_typer"
      pattern: "add_typer.*collection"
---

<objective>
Collection viewer with sorting, detail view, codex tracking, and creature renaming.

Purpose: Players can browse their creature collection, inspect individual creatures, track codex discovery progress, and personalize creatures with nicknames.
Output: Working `devmon collection`, `devmon collection <name>`, `devmon collection codex`, and `devmon collection rename` commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-party-and-collection/07-CONTEXT.md
@.planning/phases/07-party-and-collection/07-RESEARCH.md
@.planning/phases/07-party-and-collection/07-UI-SPEC.md
@.planning/phases/07-party-and-collection/07-01-SUMMARY.md
@src/devmon/models/state.py
@src/devmon/models/creature.py
@src/devmon/engine/creature_loader.py
@src/devmon/render/creatures.py
@src/devmon/render/themes.py
@src/devmon/persistence/save.py
@src/devmon/main.py

<interfaces>
<!-- Key types and contracts from Plan 01 -->

From src/devmon/models/state.py (after Plan 01):
```python
class GameState(BaseModel):
    schema_version: int = Field(default=7, ...)
    creature_collection: list[OwnedCreature] = Field(default_factory=list)
    party: list[str] = Field(default_factory=list)
    codex_state: dict[str, str] = Field(default_factory=dict)
    # codex_state values: "encountered" | "captured". Absence = "unknown".
```

From src/devmon/render/party.py (created in Plan 02, parallel):
```python
def display_name(owned: OwnedCreature, template: CreatureTemplate) -> str:
    """Return nickname if set, else template.name."""
```
NOTE: This plan runs parallel with Plan 02. If display_name is not yet available,
implement the same logic inline: `owned.nickname if owned.nickname else template.name`.
The helper can be extracted later or imported if Plan 02 completes first.

From src/devmon/render/creatures.py:
```python
def render_creature_panel(
    template: CreatureTemplate,
    console: Console,
    theme: dict[str, str] | None = None,
    encounter_level: int | None = None,
    encounter_type: str | None = None,
    encounter_rarity: str | None = None,
) -> None: ...
```

From src/devmon/engine/creature_loader.py:
```python
def load_all_creatures() -> dict[str, CreatureTemplate]: ...
def get_creature(creature_id: str) -> CreatureTemplate: ...
```

From src/devmon/render/themes.py:
```python
RARITY_COLORS = {"common": "white", "uncommon": "green", "rare": "bright_blue", "epic": "magenta", "legendary": "bold yellow"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collection list, detail view, rename command, and main.py registration</name>
  <files>src/devmon/commands/collection.py, src/devmon/main.py, tests/test_collection.py</files>
  <behavior>
    - Test: devmon collection shows table with creature name, level, rarity, type, status columns
    - Test: collection sorts by rarity (rarest first) by default; --sort level and --sort name work
    - Test: party members show [P] badge in name column
    - Test: devmon collection <name> renders creature detail panel
    - Test: devmon collection rename sets nickname and persists
    - Test: rename validation rejects empty string and >20 chars
    - Test: collection command registered in main.py
    - Test: rarity colors applied to name cells
  </behavior>
  <action>
    1. Create `src/devmon/commands/collection.py` (NEW):

       **Rarity sort order constant:**
       ```python
       RARITY_ORDER = {"legendary": 0, "epic": 1, "rare": 2, "uncommon": 3, "common": 4}
       ```

       **Typer app setup:**
       ```python
       app = typer.Typer()
       ```

       **Collection list (callback):**
       - `@app.callback(invoke_without_command=True)`
       - Signature: `collection_cmd(ctx: typer.Context, name: Optional[str] = typer.Argument(None), sort: str = typer.Option("rarity", "--sort", help="Sort by rarity|level|name"))`
       - Guard: `if ctx.invoked_subcommand is not None: return`
       - Load state. If None, print "No save file found." and return.
       - If `name` is provided: delegate to detail view (see below)
       - If no name: render collection table

       **Collection table rendering:**
       - Rich Table titled "Your Collection", box.SIMPLE
       - 6 columns per UI-SPEC: # (3 chars, dim white), Name (22 chars), Rarity (10 chars), Level (5 chars, white), Type (10 chars, dim white), Status (10 chars)
       - If creature_collection is empty: print "No creatures captured yet. Use 'devmon battle' to start your collection." dim white, then return
       - For each creature in sorted order:
         - Look up template via get_creature(owned.template_id)
         - Name cell: `display_name(owned, template)` or inline `owned.nickname if owned.nickname else template.name`. Style with RARITY_COLORS[template.rarity]. If template_id in state.party, append " [P]" styled "dim cyan" per D-07
         - Rarity cell: template.rarity.title() styled RARITY_COLORS[template.rarity]
         - Level: f"Lv.{owned.level}"
         - Type: template.type styled dim white
         - Status: "FAINTED" bold red if is_fainted, else "OK" dim white
       - Sort logic: if sort=="rarity": key=RARITY_ORDER[template.rarity]; if sort=="level": key=-owned.level (highest first); if sort=="name": key=display_name.lower()
       - Print table

       **After table, print codex completeness line:**
       - Compute discovered count: number of template_ids that appear in creature_collection OR in codex_state
       - Total: len(load_all_creatures())
       - Print: `f"Codex: {discovered}/{total} discovered"` with a simple inline progress bar using Rich Progress (width=20, cyan fill) per D-10

       **Detail view** (when `name` argument provided):
       - Case-insensitive substring match against owned creatures: match against nickname first, then template name
       - If zero matches: print "No creature named '{name}' in your collection." dim white
       - If multiple matches: print list and ask to be more specific
       - If one match: call `render_creature_panel(display_template, console)` — reuse from render/creatures.py
         - Override panel title with nickname if set (D-13): Create a modified template copy with the display name: `display_template = template.model_copy(update={"name": display_name(owned, template)})` before calling render_creature_panel(). This ensures the panel title shows the nickname, not the species name.
         - Below panel, print party status:
           - If in party: `f"  Party slot: {slot_number}"` styled dim cyan (find slot number from state.party.index(template_id) + 1)
           - If not in party: "  Not in active party." styled dim white
           - If fainted: "  Status: FAINTED -- needs rest before next battle." styled bold red

       **Rename subcommand:**
       - `@app.command("rename")`
       - Signature: `rename_cmd(creature: Optional[str] = typer.Argument(None), new_name: Optional[str] = typer.Argument(None))`
       - Load state. If None, print "No save file found." and return.
       - If creature_collection empty: print "No creatures captured yet." and return.

       **Interactive mode** (creature arg is None):
       - Print numbered list of owned creatures: `  [N] {display_name}  LVL {level}  ({rarity})`
       - Prompt: "Which creature to rename [1-N]: " bold white via input()
       - On 0 or empty: print "Rename cancelled." dim white, return
       - On invalid: re-prompt once, then abort with "Rename cancelled."
       - Then prompt: "New name (max 20 chars): " bold white via input()

       **Direct mode** (both args provided):
       - Case-insensitive substring match creature name
       - If no match: print "No creature named '{creature}' in your collection."
       - If multiple: list matches

       **Validation** (both modes, per D-12):
       - If new_name is empty or whitespace-only: print "Name cannot be empty. Please enter a name." and return
       - If len(new_name) > 20: print "Name must be 20 characters or fewer." and return

       **Assignment:**
       - Get old display name for confirmation message
       - Set `owned.nickname = new_name`
       - Save state
       - Print: "{old_name} renamed to {new_name}." styled white

    2. In `src/devmon/main.py`:
       - Add import: `from devmon.commands import collection as collection_cmd_mod`
       - Add registration: `app.add_typer(collection_cmd_mod.app, name="collection")`

    3. Create `tests/test_collection.py` (NEW):
       - Fixture `collection_state`: GameState with 3 creatures (bugbyte, glitchcat, emberfox), party=["bugbyte"], codex_state={"netshark": "encountered"}
       - `test_collection_cmd_importable`: Import collection.app, assert it exists
       - `test_collection_registered_in_main`: Import main app, assert "collection" in registered commands
       - `test_collection_shows_table`: Invoke via CliRunner, assert "Your Collection" in output, creature names present
       - `test_collection_sort_rarity`: Default sort, assert legendary/epic appear before common in output
       - `test_collection_sort_level`: --sort level, assert higher levels first
       - `test_collection_sort_name`: --sort name, assert alphabetical
       - `test_collection_party_badge`: Assert "[P]" appears next to party creature name
       - `test_collection_rarity_colors`: Use Console(record=True) or check output contains rarity-styled names
       - `test_collection_detail_renders_panel`: Invoke `collection Bugbyte`, assert creature panel content appears (ASCII art, stats)
       - `test_collection_detail_not_found`: Invoke `collection NonExistent`, assert "No creature named" error
       - `test_rename_persists`: Invoke rename (direct mode), reload state, assert nickname saved
       - `test_rename_empty_rejected`: Attempt rename with empty string, assert "Name cannot be empty"
       - `test_rename_too_long_rejected`: Attempt rename with 21+ chars, assert "20 characters or fewer"
       - `test_collection_empty_state`: Empty creature_collection, assert "No creatures captured yet" message
       - `test_codex_progress_line`: Assert "Codex:" and "/25" in collection output
  </action>
  <verify>
    <automated>uv run pytest tests/test_collection.py -x -v</automated>
  </verify>
  <done>Collection table displays all owned creatures sorted by rarity (default), level, or name per D-05, D-06. Party members marked with [P] per D-07. Detail view shows full creature panel per COLL-02. Rename validates and persists per D-11, D-12. Codex progress line visible per D-10. Collection registered in main.py per CLI-04. Rarity colors applied per UI-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex subcommand with 3-state discovery tracking</name>
  <files>src/devmon/commands/collection.py, tests/test_collection.py</files>
  <behavior>
    - Test: devmon collection codex lists all 25 creatures
    - Test: unknown creatures show "???" for name and type, "Unseen" for discovery
    - Test: encountered creatures show name (dimmed), "Encountered" state
    - Test: captured creatures show name (full color), "Captured" state
    - Test: codex header shows "Codex: N/25 discovered" with progress bar
  </behavior>
  <action>
    1. Add `codex_cmd` subcommand to `src/devmon/commands/collection.py`:
       - `@app.command("codex")`
       - Signature: `codex_cmd() -> None`
       - Load state. If None, print "No save file found." and return.
       - Load all creature templates via `load_all_creatures()` — gives dict[str, CreatureTemplate]

       **Compute discovery state for each creature (per Pitfall 3 recommendation from RESEARCH.md):**
       - For each template_id in all creatures:
         - If template_id is in `[c.template_id for c in state.creature_collection]` -> "captured"
         - Elif template_id is in `state.codex_state` with value "encountered" -> "encountered"
         - Else -> "unknown"
       - Count discovered = number of creatures that are NOT "unknown"

       **Codex progress header:**
       - Use Rich Progress to render: "Codex: {discovered}/25 discovered [bar]"
       - Progress bar: width=20, complete_style="cyan", finished_style="cyan"
       - Print this ABOVE the table

       **Codex table:**
       - Rich Table titled "Creature Codex", box.SIMPLE
       - 4 columns per UI-SPEC: # (3 chars, dim white), Name (22 chars), Rarity (10 chars), Discovery (12 chars)
       - Sort: by creature ID order (canonical order from creature JSON files — sorted by template_id alphabetically, or by the order load_all_creatures returns them)
       - For each creature in sorted order:
         - # column: row number
         - If "unknown" per D-09:
           - Name: "???" styled "dim white"
           - Rarity: "???" styled "dim white"
           - Discovery: "Unseen" styled "dim white"
         - If "encountered":
           - Name: template.name styled with RARITY_COLORS but dimmed (append "dim" to style, e.g. "dim green" for uncommon)
           - Rarity: template.rarity.title() styled with RARITY_COLORS dimmed
           - Discovery: "Encountered" styled "dim cyan"
         - If "captured":
           - Name: template.name styled RARITY_COLORS (full brightness)
           - Rarity: template.rarity.title() styled RARITY_COLORS (full)
           - Discovery: "Captured" styled "dim green"
       - Print table

    2. Add tests to `tests/test_collection.py`:
       - `test_codex_lists_all_creatures`: Invoke `collection codex`, assert output has 25 rows (or at least "Creature Codex" title and "/25")
       - `test_codex_unknown_shows_question_marks`: State with no encounters, assert "???" appears in output for unknown creatures and "Unseen" state shown
       - `test_codex_encountered_shows_name`: State with codex_state={"bugbyte": "encountered"}, assert "Bugbyte" appears (not "???") and "Encountered" state
       - `test_codex_captured_shows_full`: State with bugbyte in creature_collection, assert "Bugbyte" in output and "Captured" state
       - `test_codex_progress_bar`: Assert "Codex:" header with discovered count
  </action>
  <verify>
    <automated>uv run pytest tests/test_collection.py -x -v</automated>
  </verify>
  <done>Codex lists all 25 creatures with 3-state discovery tracking per D-08. Unknown creatures show "???" per D-09. Completeness counter with progress bar per D-10. Discovery state derived dynamically from creature_collection and codex_state per COLL-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> rename | Untrusted creature name and new nickname from CLI |
| user input -> collection detail | Untrusted name argument for substring match |
| creature JSON -> codex | Template IDs from JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | T (Tampering) | collection.py rename | mitigate | Validate name length (max 20), reject empty strings per D-12 |
| T-07-07 | I (Info Disclosure) | collection.py | mitigate | Never display capture_rate in collection table, detail view, or codex (HARD RULE) |
| T-07-08 | S (Spoofing) | collection.py detail | accept | Substring match could match unintended creature, but user is shown match and can re-try. Single-player game, low risk. |
| T-07-09 | D (Denial of Service) | collection.py rename interactive | mitigate | Re-prompt once on invalid input then abort with "Rename cancelled." |
</threat_model>

<verification>
- `uv run pytest tests/test_collection.py -x -v` passes
- `uv run pytest` full suite passes
- `devmon collection` shows captured creatures with rarity colors
- `devmon collection Bugbyte` shows detail panel
- `devmon collection codex` shows all 25 creatures with discovery state
- `devmon collection rename Bugbyte "Sparky"` renames and persists
</verification>

<success_criteria>
- Collection table shows all owned creatures with rarity colors per D-05, UI-05
- Sort by rarity (rarest first), level, or name per D-06
- Party members marked with [P] per D-07
- Detail view reuses render_creature_panel per COLL-02
- Codex shows all 25 creatures with 3-state discovery per D-08, COLL-03
- Unknown entries show "???" per D-09
- Codex completeness counter per D-10
- Rename validates (max 20, non-empty) per D-12
- Rename persists per COLL-04
- Collection registered in main.py per CLI-04
</success_criteria>

<output>
After completion, create `.planning/phases/07-party-and-collection/07-03-SUMMARY.md`
</output>
