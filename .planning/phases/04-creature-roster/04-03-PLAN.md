---
phase: 04-creature-roster
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - scripts/show_creatures.py
autonomous: false
requirements:
  - CREA-01
  - CREA-02
  - CREA-03
  - CREA-04

must_haves:
  truths:
    - "ASCII art renders visually clean in an 80-column terminal"
    - "Rarity colors are visually distinguishable"
    - "Creature panels do not overflow or corrupt terminal display"
    - "Full test suite passes"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete creature roster rendering. Confirm ASCII art quality, rarity color distinction, panel layout, and full test suite health.

Purpose: Visual quality cannot be automated — human eyes confirm that 25 creatures look good in the terminal.
Output: Verified Phase 4 deliverables ready for Phase 5 encounter system.
</objective>

<execution_context>
@C:/Users/flopp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/flopp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-creature-roster/04-CONTEXT.md
@.planning/phases/04-creature-roster/04-UI-SPEC.md
@.planning/phases/04-creature-roster/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate creature gallery display script</name>
  <files>scripts/show_creatures.py</files>
  <read_first>
    - src/devmon/render/creatures.py (render_creature_panel function signature)
    - src/devmon/engine/creature_loader.py (load_all_creatures function)
  </read_first>
  <action>
Create a temporary script `scripts/show_creatures.py` that displays all 25 creatures in the terminal for human review. The script should:

1. Import `load_all_creatures` from `devmon.engine.creature_loader`
2. Import `render_creature_panel` from `devmon.render.creatures`
3. Import `Console` from `rich.console`
4. Create Console with `width=80` (CREA-03: 80-column terminal)
5. Load all creatures, sort by rarity tier (common first, legendary last)
6. For each creature: call `render_creature_panel(template, console)`
7. Print a summary line at end: "Total: {N} creatures | {rarity_counts}"

Run with: `uv run python scripts/show_creatures.py`

Also run the full test suite: `uv run pytest tests/ -v`
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/show_creatures.py` exists
    - `uv run python scripts/show_creatures.py` runs without error
    - `uv run pytest tests/ -x -q` passes
  </acceptance_criteria>
  <done>Gallery script created and runs, full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of creature roster rendering</name>
  <files></files>
  <action>
Present the creature gallery to the user for visual approval. Run `uv run python scripts/show_creatures.py` and display output. Run `uv run pytest tests/ -v` to confirm full suite health.
  </action>
  <what-built>Complete creature roster: 25 creatures across 5 rarity tiers with ASCII art, stats, flavor text, and rarity-colored Rich panels. CreatureTemplate/OwnedCreature models, creature_loader with DEVMON_HOME override, schema v4 migration, and render_creature_panel function.</what-built>
  <how-to-verify>
    1. Run `uv run python scripts/show_creatures.py` in an 80-column terminal
    2. Verify each creature panel:
       - ASCII art is recognizable and visually clean (no garbled characters, no overflow)
       - Rarity border colors are distinct: white (common), green (uncommon), blue (rare), magenta (epic), yellow (legendary)
       - Stat block shows HP, ATK, DEF, SPD, Type, Capture rate
       - Flavor text is readable and humorous
       - Panel does not extend beyond 80 columns
    3. Verify rarity distribution in the summary line: 8C, 7U, 5R, 3E, 2L
    4. Run `uv run pytest tests/ -v` and confirm all tests pass
    5. Optional: edit one creature JSON file (change a stat), re-run gallery, confirm change is reflected immediately (CREA-04)
  </how-to-verify>
  <verify>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <done>Human approves visual quality of all 25 creature panels.</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe specific creatures that need art/stat/text adjustments.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan only — no new code with trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new attack surface in this verification plan |
</threat_model>

<verification>
1. `uv run python scripts/show_creatures.py` renders all 25 creatures cleanly
2. `uv run pytest tests/ -v` — full suite green
3. Human confirms visual quality of ASCII art and panel layout
</verification>

<success_criteria>
- Human approves creature art quality and panel rendering
- All 25 creatures display without terminal overflow at 80 columns
- Rarity colors visually distinguishable
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-creature-roster/04-03-SUMMARY.md`
</output>
