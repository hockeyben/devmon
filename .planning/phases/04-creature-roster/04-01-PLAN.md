---
phase: 04-creature-roster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_creatures.py
  - src/devmon/models/creature.py
  - src/devmon/models/state.py
  - src/devmon/persistence/migrations.py
  - src/devmon/data/__init__.py
  - src/devmon/data/creatures/__init__.py
  - src/devmon/engine/creature_loader.py
autonomous: true
requirements:
  - CREA-01
  - CREA-02
  - CREA-03
  - CREA-04

must_haves:
  truths:
    - "CreatureTemplate model validates all required fields including type, rarity, stats, capture_rate, ascii_art, evolution stubs"
    - "OwnedCreature model stores template_id reference, not embedded template data"
    - "GameState schema_version is 4 and migration from v3 adds creature_collection=[]"
    - "creature_loader loads JSON files from bundled package data and DEVMON_HOME override"
    - "Tests exist for all CREA-01 through CREA-04 behaviors"
  artifacts:
    - path: "src/devmon/models/creature.py"
      provides: "CreatureTemplate and OwnedCreature Pydantic v2 models"
      exports: ["CreatureTemplate", "OwnedCreature", "CreatureType", "CreatureRarity"]
    - path: "src/devmon/engine/creature_loader.py"
      provides: "Creature loading from JSON with DEVMON_HOME override"
      exports: ["load_all_creatures", "get_creature"]
    - path: "src/devmon/data/__init__.py"
      provides: "Package marker for importlib.resources"
    - path: "src/devmon/data/creatures/__init__.py"
      provides: "Package marker for importlib.resources"
    - path: "tests/test_creatures.py"
      provides: "Test coverage for CREA-01 through CREA-04"
      min_lines: 80
  key_links:
    - from: "src/devmon/engine/creature_loader.py"
      to: "src/devmon/models/creature.py"
      via: "CreatureTemplate.model_validate()"
      pattern: "CreatureTemplate\\.model_validate"
    - from: "src/devmon/models/state.py"
      to: "src/devmon/models/creature.py"
      via: "OwnedCreature import for creature_collection field"
      pattern: "creature_collection.*list\\[OwnedCreature\\]"
    - from: "src/devmon/engine/creature_loader.py"
      to: "src/devmon/data/creatures"
      via: "importlib.resources.files()"
      pattern: 'files\\("devmon\\.data\\.creatures"\\)'
---

<objective>
Create the creature data layer: Pydantic v2 models (CreatureTemplate, OwnedCreature), schema v4 migration, creature loader with importlib.resources + DEVMON_HOME override, and test scaffold for all Phase 4 requirements.

Purpose: Establish the complete creature infrastructure so Plan 02 can populate 25 creature JSON files against a validated schema and render them.
Output: Models, loader, migration, package markers, and comprehensive test file.
</objective>

<execution_context>
@C:/Users/flopp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/flopp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-creature-roster/04-CONTEXT.md
@.planning/phases/04-creature-roster/04-RESEARCH.md
@.planning/phases/04-creature-roster/04-UI-SPEC.md
@.planning/phases/04-creature-roster/04-VALIDATION.md
@src/devmon/models/state.py
@src/devmon/persistence/migrations.py
@src/devmon/persistence/save.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/devmon/models/state.py:
```python
class PlayerProfile(BaseModel):
    name: str
    level: int = 1
    xp: int = 0
    currency: int = 0
    total_sessions: int = 0
    total_commands: int = 0
    total_creatures_seen: int = 0
    total_creatures_captured: int = 0
    battles_won: int = 0
    streak_count: int = 0
    last_active_date: Optional[date] = None
    streak_grace_used: bool = False
    session_xp_earned: int = 0
    level_up_pending: bool = False
    pending_level_value: int = 0

class GameState(BaseModel):
    schema_version: int = Field(default=3, ...)
    player: PlayerProfile
```

From src/devmon/persistence/migrations.py:
```python
CURRENT_VERSION = 3
def migrate(data: dict) -> dict: ...
def _migrate_2_to_3(data: dict) -> dict: ...
```

From tests/conftest.py:
```python
@pytest.fixture
def tmp_save_dir(tmp_path): ...   # Sets DEVMON_HOME to isolated dir
@pytest.fixture
def tmp_devmon_home(tmp_path): ... # Sets DEVMON_HOME to isolated dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CreatureTemplate and OwnedCreature Pydantic v2 models</name>
  <files>src/devmon/models/creature.py</files>
  <read_first>
    - src/devmon/models/state.py (pattern to follow for Pydantic models)
    - .planning/phases/04-creature-roster/04-RESEARCH.md (Pattern 1 and Pattern 2 sections)
    - .planning/phases/04-creature-roster/04-UI-SPEC.md (ASCII Art Production Contract)
  </read_first>
  <behavior>
    - CreatureTemplate with all required fields validates a complete creature dict
    - CreatureTemplate rejects invalid rarity (not in common/uncommon/rare/epic/legendary)
    - CreatureTemplate rejects invalid type (not in the 8 elemental types)
    - CreatureTemplate rejects capture_rate outside 0.0-1.0
    - CreatureTemplate rejects base_hp/base_attack/base_defense/base_speed < 1
    - CreatureTemplate model_validator rejects ascii_art lines > 40 chars
    - CreatureTemplate model_validator rejects ascii_art with < 3 or > 20 lines
    - OwnedCreature stores template_id string, not embedded template fields
    - OwnedCreature round-trips through JSON (model_dump_json -> model_validate_json)
    - CreatureType is Literal["Fire", "Water", "Earth", "Electric", "Shadow", "Ice", "Psychic", "Nature"] (per D-02)
    - CreatureRarity is Literal["common", "uncommon", "rare", "epic", "legendary"]
  </behavior>
  <action>
Create `src/devmon/models/creature.py` with the following exact structure:

1. Module docstring following state.py pattern: explain this is a pure data container, no imports from commands/render/engine.

2. Define type aliases (per D-01, D-02):
   ```python
   CreatureType = Literal["Fire", "Water", "Earth", "Electric", "Shadow", "Ice", "Psychic", "Nature"]
   CreatureRarity = Literal["common", "uncommon", "rare", "epic", "legendary"]
   ```

3. Define `CreatureTemplate(BaseModel)` with these exact fields (per D-01 through D-08, CREA-02):
   - `id: str` — snake_case identifier matching JSON filename stem
   - `name: str` — display name (e.g., "EmberFox")
   - `species: str` — flavor species name (e.g., "Flame Fox")
   - `rarity: CreatureRarity`
   - `type: CreatureType` (per D-01: single type)
   - `level_range: tuple[int, int]` — min/max level for wild encounters
   - `base_hp: int = Field(ge=1)`
   - `base_attack: int = Field(ge=1)`
   - `base_defense: int = Field(ge=1)`
   - `base_speed: int = Field(ge=1)`
   - `capture_rate: float = Field(ge=0.0, le=1.0)` (per D-04)
   - `flavor_text: str` (per D-13)
   - `ascii_art: list[str]` — plain ASCII lines, NO Rich markup (per D-07)
   - `primary_color: str` — Rich style string (per D-08)
   - `accent_color: str` — Rich style string (per D-08)
   - `evolves_from: Optional[str] = None` (per D-05: creature id or None)
   - `evolves_to: Optional[str] = None` (per D-05: creature id or None)

4. Add `@model_validator(mode="after")` on CreatureTemplate to enforce ASCII art constraints (per UI-SPEC):
   - Each line <= 40 chars, raise ValueError with f"ASCII art line {i+1} exceeds 40-char limit ({len(line)} chars)"
   - Total lines >= 3, raise ValueError "ASCII art must have at least 3 lines"
   - Total lines <= 20, raise ValueError "ASCII art must not exceed 20 lines"

5. Define `OwnedCreature(BaseModel)` with (per RESEARCH Pattern 2):
   - `template_id: str` — references CreatureTemplate.id (NOT embedded template data — Pitfall 4)
   - `nickname: Optional[str] = None` (COLL-04 stub)
   - `level: int = 1`
   - `xp: int = 0`
   - `current_hp: Optional[int] = None` — None means max HP
   - `is_fainted: bool = False` (PRTY-04 stub)
  </action>
  <verify>
    <automated>uv run python -c "from devmon.models.creature import CreatureTemplate, OwnedCreature, CreatureType, CreatureRarity; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/devmon/models/creature.py` exists
    - `grep -c "CreatureType = Literal" src/devmon/models/creature.py` returns 1
    - `grep -c "CreatureRarity = Literal" src/devmon/models/creature.py` returns 1
    - `grep -c "class CreatureTemplate" src/devmon/models/creature.py` returns 1
    - `grep -c "class OwnedCreature" src/devmon/models/creature.py` returns 1
    - `grep -c "evolves_from" src/devmon/models/creature.py` returns at least 1 (D-05)
    - `grep -c "evolves_to" src/devmon/models/creature.py` returns at least 1 (D-05)
    - `grep -c "primary_color" src/devmon/models/creature.py` returns at least 1 (D-08)
    - `grep -c "accent_color" src/devmon/models/creature.py` returns at least 1 (D-08)
    - `grep -c "model_validator" src/devmon/models/creature.py` returns at least 1
    - `grep -c "template_id" src/devmon/models/creature.py` returns at least 1 (not embedding template data)
    - OwnedCreature does NOT have fields base_hp, base_attack, base_defense, base_speed, type, flavor_text (Pitfall 4)
  </acceptance_criteria>
  <done>CreatureTemplate and OwnedCreature models exist with all required fields, Pydantic validators enforce art constraints and stat ranges, evolution stubs present, color hint fields present.</done>
</task>

<task type="auto">
  <name>Task 2: Schema v4 migration + creature_loader + package markers + test scaffold</name>
  <files>
    src/devmon/models/state.py
    src/devmon/persistence/migrations.py
    src/devmon/data/__init__.py
    src/devmon/data/creatures/__init__.py
    src/devmon/engine/creature_loader.py
    tests/test_creatures.py
  </files>
  <read_first>
    - src/devmon/models/state.py (current schema_version=3, needs bump to 4)
    - src/devmon/persistence/migrations.py (current CURRENT_VERSION=3, migration pattern)
    - src/devmon/engine/__init__.py (existing engine package)
    - tests/conftest.py (tmp_save_dir and tmp_devmon_home fixtures)
    - src/devmon/models/creature.py (just created in Task 1 — need OwnedCreature import)
    - .planning/phases/04-creature-roster/04-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 3, Pitfall 5)
  </read_first>
  <action>
**A) Modify `src/devmon/models/state.py`:**
1. Add import: `from devmon.models.creature import OwnedCreature`
2. Add field to `GameState`: `creature_collection: list[OwnedCreature] = Field(default_factory=list)`
3. Change `schema_version` default from 3 to 4: `schema_version: int = Field(default=4, description="Save file schema version for migration support")`

**B) Modify `src/devmon/persistence/migrations.py`:**
1. Change `CURRENT_VERSION = 3` to `CURRENT_VERSION = 4`
2. Add migration function:
   ```python
   def _migrate_3_to_4(data: dict) -> dict:
       """Version 3 -> 4: Phase 4 adds creature_collection to GameState."""
       data.setdefault("creature_collection", [])
       data["schema_version"] = 4
       return data
   ```
3. Add `3: _migrate_3_to_4` to the `migrations` dict inside `migrate()`

**C) Create `src/devmon/data/__init__.py`:**
Empty file with a single docstring: `"""DevMon bundled data package — required for importlib.resources.files()."""`

**D) Create `src/devmon/data/creatures/__init__.py`:**
Empty file with a single docstring: `"""DevMon creature JSON data package — required for importlib.resources.files()."""`

**E) Create `src/devmon/engine/creature_loader.py`:**
Follow RESEARCH Pattern 4 exactly:
1. Module docstring explaining purpose: loads creature templates from bundled JSON + DEVMON_HOME override.
2. `_iter_creature_files() -> dict[str, str]`: Returns dict of {filename: json_text}. Uses `importlib.resources.files("devmon.data.creatures")` for bundled data. Checks `DEVMON_HOME/creatures/` for override files (D-10). Merges: DEVMON_HOME files override bundled files by matching filename.
3. `load_all_creatures() -> dict[str, CreatureTemplate]`: Calls `_iter_creature_files()`, validates each with `CreatureTemplate.model_validate()`, collects errors, raises ValueError listing all errors if any fail (D-11 fail fast). Returns dict keyed by `template.id`.
4. `get_creature(creature_id: str) -> CreatureTemplate`: Convenience function that calls `load_all_creatures()` and returns the creature by id. Raises `KeyError` with helpful message listing available IDs if not found.
5. Do NOT use `lru_cache` at module level or call loader at import time (Pitfall 5). The `get_creature` function may optionally use a module-level `_registry: dict | None = None` pattern that `load_all_creatures()` populates, but only on explicit call.

**F) Create `tests/test_creatures.py`:**
Write a comprehensive test file with the following tests. Tests that require creature JSON data files (which don't exist yet in Wave 1) should use `pytest.mark.xfail(strict=True, reason="Phase 4 Plan 02: creature JSON data files not yet created")`. Tests that can run against just the models should pass immediately.

Passing tests (models only):
- `test_creature_template_valid`: Construct a valid CreatureTemplate dict and assert model_validate succeeds
- `test_creature_template_invalid_rarity`: Assert ValidationError on rarity="godlike"
- `test_creature_template_invalid_type`: Assert ValidationError on type="Lava"
- `test_creature_template_capture_rate_bounds`: Assert ValidationError on capture_rate=1.5 and capture_rate=-0.1
- `test_creature_template_stat_minimums`: Assert ValidationError on base_hp=0
- `test_creature_template_art_line_too_wide`: Assert ValidationError when ascii_art has a line > 40 chars
- `test_creature_template_art_too_few_lines`: Assert ValidationError when ascii_art has < 3 lines
- `test_creature_template_art_too_many_lines`: Assert ValidationError when ascii_art has > 20 lines
- `test_owned_creature_round_trip`: Create OwnedCreature, dump to JSON, validate back, assert fields match
- `test_owned_creature_no_template_fields`: Assert OwnedCreature model_fields does NOT contain "base_hp", "type", "flavor_text"
- `test_schema_version_is_4`: Assert `GameState().schema_version == 4` (update from existing test pattern)
- `test_migrate_3_to_4`: Call `_migrate_3_to_4({"schema_version": 3, "player": {...}})` and assert creature_collection=[] added
- `test_migrate_3_to_4_preserves_existing`: Call with creature_collection already present, assert not overwritten

xfail tests (need creature JSON files from Plan 02):
- `test_roster_count`: `load_all_creatures()` returns exactly 25 creatures (CREA-01)
- `test_rarity_distribution`: Count by rarity: 8 common, 7 uncommon, 5 rare, 3 epic, 2 legendary (D-14)
- `test_all_creature_types_used`: All 8 elemental types appear at least once across the roster (D-02)
- `test_devmon_home_override`: Write a creature JSON to tmp_devmon_home/creatures/, call load_all_creatures(), verify the override is loaded
- `test_fallback_to_bundled`: With DEVMON_HOME set but no creatures/ subdir, bundled data loads normally
- `test_invalid_creature_json_fails_fast`: Write invalid JSON to override dir, assert ValueError from loader (D-11)

Each test should use a helper fixture `_sample_creature_dict()` that returns a valid creature dict for reuse.
  </action>
  <verify>
    <automated>uv run pytest tests/test_creatures.py -x -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CURRENT_VERSION = 4" src/devmon/persistence/migrations.py` matches
    - `grep "schema_version.*default=4" src/devmon/models/state.py` matches
    - `grep "creature_collection" src/devmon/models/state.py` matches
    - `grep "_migrate_3_to_4" src/devmon/persistence/migrations.py` matches
    - File `src/devmon/data/__init__.py` exists
    - File `src/devmon/data/creatures/__init__.py` exists
    - File `src/devmon/engine/creature_loader.py` exists
    - `grep "load_all_creatures" src/devmon/engine/creature_loader.py` matches
    - `grep "get_creature" src/devmon/engine/creature_loader.py` matches
    - `grep 'files("devmon.data.creatures")' src/devmon/engine/creature_loader.py` matches
    - `grep "DEVMON_HOME" src/devmon/engine/creature_loader.py` matches
    - File `tests/test_creatures.py` exists with at least 15 test functions
    - All non-xfail tests in test_creatures.py pass
    - xfail tests are marked strict=True
    - `uv run pytest tests/ -x -q` passes (full suite including prior phases)
  </acceptance_criteria>
  <done>Schema v4 migration works, creature_loader loads from bundled data + DEVMON_HOME override, package markers exist for importlib.resources, comprehensive test scaffold covers all CREA requirements with xfail stubs for data-dependent tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEVMON_HOME/creatures/*.json | User-supplied creature data files — untrusted JSON input |
| Bundled data/creatures/*.json | Package-bundled data — trusted (ships with code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | creature_loader.py: DEVMON_HOME override | mitigate | Pydantic v2 model_validate() enforces schema on all JSON input; ValidationError on any invalid field type/value/range |
| T-04-02 | Tampering | creature_loader.py: malformed JSON | mitigate | json.loads() raises JSONDecodeError; loader catches and includes filename in error message |
| T-04-03 | Information Disclosure | creature_loader.py: directory traversal via filename | accept | DEVMON_HOME/creatures/ glob uses *.json pattern — filenames are not user-controlled input, they're local filesystem files the user already owns |
| T-04-04 | Denial of Service | creature_loader.py: extremely large JSON file | accept | Local-only CLI tool; user would be DoS-ing themselves. No mitigation needed. |
</threat_model>

<verification>
1. `uv run pytest tests/test_creatures.py -x -v` — all non-xfail tests pass
2. `uv run pytest tests/ -x -q` — full suite green (including prior phases)
3. `uv run python -c "from devmon.models.creature import CreatureTemplate, OwnedCreature; print('OK')"` — imports work
4. `uv run python -c "from devmon.engine.creature_loader import load_all_creatures; print('OK')"` — loader imports without triggering load
</verification>

<success_criteria>
- CreatureTemplate validates all CREA-02 fields with proper constraints
- OwnedCreature references template_id only (no embedded data)
- GameState schema v4 with creature_collection field
- Migration v3->v4 adds creature_collection=[] using setdefault pattern
- creature_loader.py loads from importlib.resources + DEVMON_HOME override
- Test scaffold covers all CREA-01 through CREA-04 behaviors
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-creature-roster/04-01-SUMMARY.md`
</output>
