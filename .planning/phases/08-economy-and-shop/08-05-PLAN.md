---
phase: 08-economy-and-shop
plan: 05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified: []
autonomous: false
requirements:
  - ECON-01
  - ECON-02
  - ECON-03
  - ECON-04
  - CLI-05
  - CLI-06
must_haves:
  truths:
    - "All economy features verified visually by human tester"
    - "Shop displays correctly with category grouping and Bits balance"
    - "Items command shows inventory with correct quantities"
    - "Battle capsule and item sub-menus render and function correctly"
    - "Status shows Bits label and XP booster indicator when active"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 8 economy features: shop, items, battle integration, status display, and XP booster.

Purpose: Visual and functional verification that cannot be automated — shop layout, purchase flow, battle sub-menus, status display updates.
Output: Verified or issues identified for gap closure
</objective>

<execution_context>
@C:\Users\flopp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\flopp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-economy-and-shop/08-CONTEXT.md
@.planning/phases/08-economy-and-shop/08-UI-SPEC.md
@.planning/phases/08-economy-and-shop/08-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of all Phase 8 economy features</name>
  <files></files>
  <action>
    Present the 7 verification tests below to the human tester. Complete economy and shop system: devmon shop (interactive + --buy), devmon items (inventory + --use), battle capsule sub-menu, battle items sub-menu, Bits currency display, XP booster multiplier.

    **Prerequisite:** Ensure you have a save file with some currency. If starting fresh, run a few battles first to earn Bits, or manually edit the save file to set `"currency": 200`.

    **Test 1: Shop Display (D-01, D-03, D-06, D-21)**
    1. Run `uv run devmon shop`
    2. Verify: Shop header shows "Bits: {N}" with your balance
    3. Verify: Items grouped into Capsules, Potions, Boosters sections
    4. Verify: Each item shows number, name, price, and current quantity
    5. Verify: Items you can't afford appear grayed out
    6. Verify: Master Capsule shows "(earn only)" with no purchase number

    **Test 2: Purchase Flow (D-02, D-05)**
    1. In the shop, enter a number to buy an affordable item
    2. Verify: Green "Purchased" panel shows item name, cost, and new balance
    3. Verify: Shop re-displays with updated balance and quantity
    4. Try buying an item you can't afford — verify error message
    5. Press 'q' to quit
    6. Run `uv run devmon shop --buy basic_capsule` — verify one-shot purchase works

    **Test 3: Items Inventory (D-14)**
    1. Run `uv run devmon items`
    2. Verify: Items displayed grouped by category (Capsules, Potions, Boosters)
    3. Verify: Items with qty > 0 are bright, qty = 0 are dimmed
    4. Verify: Layout mirrors shop grouping

    **Test 4: XP Booster (D-08, D-17)**
    1. Run `uv run devmon items --use xp-booster` (must own one)
    2. Verify: Message "XP Booster active! 1.5x XP for 30 minutes." appears in magenta
    3. Run `uv run devmon status`
    4. Verify: "XP Boost  ACTIVE (N min)" row appears in magenta

    **Test 5: Status Bits Display (D-11, D-13)**
    1. Run `uv run devmon status`
    2. Verify: Currency shows "{N} Bits" (not "G")

    **Test 6: Battle Capsule Sub-Menu (D-18)**
    1. Trigger an encounter and run `uv run devmon battle`
    2. Select [3] Capture
    3. Verify: Sub-menu shows owned capsule types with quantities
    4. Select a capsule tier and verify capture animation uses correct capsule name
    5. Press 'b' to verify back returns to action menu without consuming turn

    **Test 7: Battle Items Sub-Menu (D-15)**
    1. During battle, select [5] Items
    2. Verify: Sub-menu shows usable potions/revives with quantities
    3. Use a Small Potion — verify HP increases and turn is consumed
    4. Verify: "[5] Items" in action menu is now active (no "coming soon" text)
  </action>
  <verify>Human tester confirms all 7 test scenarios visually.</verify>
  <done>All 7 verification tests approved by human tester, or issues documented for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — verification only.

## STRIDE Threat Register

No new threats — verification plan.
</threat_model>

<verification>
Human tester confirms all 7 test scenarios pass visually.
</verification>

<success_criteria>
- All 7 verification tests pass
- No visual rendering issues
- No functional bugs in purchase, item use, or battle integration
- XP booster indicator displays correctly in status
</success_criteria>

<output>
After completion, create `.planning/phases/08-economy-and-shop/08-05-SUMMARY.md`
</output>
