---
phase: 06-battle-and-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/devmon/data/creatures/bugbyte.json
  - src/devmon/data/creatures/ember_fox.json
  - src/devmon/data/creatures/stackcat.json
  - src/devmon/data/creatures/frost_fang.json
  - src/devmon/data/creatures/shade_wisp.json
  - src/devmon/data/creatures/boulder_bash.json
  - src/devmon/data/creatures/char_mander.json
  - src/devmon/data/creatures/drift_yeti.json
  - src/devmon/data/creatures/gloom_bat.json
  - src/devmon/data/creatures/hex_owl.json
  - src/devmon/data/creatures/inferno_drake.json
  - src/devmon/data/creatures/kraken_byte.json
  - src/devmon/data/creatures/mind_moth.json
  - src/devmon/data/creatures/moss_golem.json
  - src/devmon/data/creatures/nullhound.json
  - src/devmon/data/creatures/quake_titan.json
  - src/devmon/data/creatures/root_ancient.json
  - src/devmon/data/creatures/storm_phoenix.json
  - src/devmon/data/creatures/thorn_sprite.json
  - src/devmon/data/creatures/tide_byte.json
  - src/devmon/data/creatures/vine_cobra.json
  - src/devmon/data/creatures/void_leviathan.json
  - src/devmon/data/creatures/volt_whisker.json
  - src/devmon/data/creatures/wave_runner.json
  - src/devmon/data/creatures/zap_ferret.json
autonomous: true
requirements:
  - CREA-06

must_haves:
  truths:
    - "All 25 creature JSON files have abilities arrays with 2-3 abilities each"
    - "Ability types match each creature's type field"
    - "Ability names are unique across all 25 creatures (75 unique names for 75 abilities)"
    - "Damage multipliers follow level tiers: level 1 = 1.2-1.5, level 5 = 1.6-2.0, level 10 = 2.0-2.5"
  artifacts:
    - path: "src/devmon/data/creatures/*.json"
      provides: "Abilities data for all 25 creatures"
      contains: "abilities"
  key_links:
    - from: "src/devmon/data/creatures/*.json"
      to: "src/devmon/models/creature.py"
      via: "Ability model validation"
      pattern: "abilities.*learn_level"
---

<objective>
Add abilities data to all 25 creature JSON files.

Purpose: Populate the creature ability pool required by CREA-06 and D-10. Each creature gets 2-3 thematically appropriate, dev-culture-themed abilities with level thresholds. This plan is pure data production -- no model or code changes (those are in Plan 01).

Output: All 25 creature JSON files updated with abilities arrays. 75 unique ability names across the roster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-battle-and-capture/06-CONTEXT.md

<interfaces>
From src/devmon/models/creature.py (after Plan 01):
```python
class Ability(BaseModel):
    name: str
    damage_multiplier: float = Field(gt=0.0)
    type: CreatureType
    learn_level: int = Field(ge=1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add abilities to all 25 creature JSON files</name>
  <files>
    src/devmon/data/creatures/bugbyte.json,
    src/devmon/data/creatures/ember_fox.json,
    src/devmon/data/creatures/stackcat.json,
    src/devmon/data/creatures/frost_fang.json,
    src/devmon/data/creatures/shade_wisp.json,
    src/devmon/data/creatures/boulder_bash.json,
    src/devmon/data/creatures/char_mander.json,
    src/devmon/data/creatures/drift_yeti.json,
    src/devmon/data/creatures/gloom_bat.json,
    src/devmon/data/creatures/hex_owl.json,
    src/devmon/data/creatures/inferno_drake.json,
    src/devmon/data/creatures/kraken_byte.json,
    src/devmon/data/creatures/mind_moth.json,
    src/devmon/data/creatures/moss_golem.json,
    src/devmon/data/creatures/nullhound.json,
    src/devmon/data/creatures/quake_titan.json,
    src/devmon/data/creatures/root_ancient.json,
    src/devmon/data/creatures/storm_phoenix.json,
    src/devmon/data/creatures/thorn_sprite.json,
    src/devmon/data/creatures/tide_byte.json,
    src/devmon/data/creatures/vine_cobra.json,
    src/devmon/data/creatures/void_leviathan.json,
    src/devmon/data/creatures/volt_whisker.json,
    src/devmon/data/creatures/wave_runner.json,
    src/devmon/data/creatures/zap_ferret.json
  </files>
  <read_first>
    src/devmon/data/creatures/bugbyte.json,
    src/devmon/data/creatures/ember_fox.json,
    src/devmon/data/creatures/stackcat.json
  </read_first>
  <action>
Add abilities arrays to all 25 creature JSON files (CREA-06, D-10).

Each creature gets 2-3 abilities with level thresholds. The ability type MUST match the creature's type. Design pattern: one ability at learn_level 1, one at learn_level 5, optionally one at learn_level 10.

Example for bugbyte.json (type: Psychic):
```json
"abilities": [
  {"name": "Glitch Strike", "damage_multiplier": 1.4, "type": "Psychic", "learn_level": 1},
  {"name": "Memory Leak", "damage_multiplier": 1.8, "type": "Psychic", "learn_level": 5},
  {"name": "Stack Overflow", "damage_multiplier": 2.2, "type": "Psychic", "learn_level": 10}
]
```

For each creature, design thematically appropriate ability names matching the creature's flavor. Use creature type for all abilities. Damage multipliers: level 1 = 1.2-1.5, level 5 = 1.6-2.0, level 10 = 2.0-2.5.

Ability names should be creative and dev-culture themed where possible (matching the project's dev-creature identity).

**CRITICAL: All 75 ability names must be unique across the entire roster.** No two creatures may share an ability name. After writing all files, verify uniqueness by grepping for duplicate names.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && python -c "
import json, glob, collections
files = glob.glob('src/devmon/data/creatures/*.json')
assert len(files) == 25, f'Expected 25 creature files, found {len(files)}'
all_names = []
for f in files:
    data = json.load(open(f))
    assert 'abilities' in data, f'{f} missing abilities'
    abilities = data['abilities']
    assert 2 <= len(abilities) <= 3, f'{f} has {len(abilities)} abilities (expected 2-3)'
    for a in abilities:
        assert a['type'] == data['type'], f'{f}: ability type {a[\"type\"]} != creature type {data[\"type\"]}'
        all_names.append(a['name'])
dupes = [n for n, c in collections.Counter(all_names).items() if c > 1]
assert not dupes, f'Duplicate ability names: {dupes}'
print(f'OK: {len(files)} creatures, {len(all_names)} unique abilities')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 25 creature JSON files contain "abilities" key
    - Each creature has 2-3 abilities
    - All ability types match their creature's type
    - All 75 ability names are unique across the entire roster (zero duplicates)
    - Damage multipliers follow tier ranges: level 1 = 1.2-1.5, level 5 = 1.6-2.0, level 10 = 2.0-2.5
    - Verification script passes with 0 errors
  </acceptance_criteria>
  <done>All 25 creatures have abilities with unique names, correct types, and tiered damage multipliers. Uniqueness verified programmatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON creature data -> Pydantic model | Abilities loaded from user-editable JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | creature JSON abilities | accept | Creature data is local single-player; user can tweak their own game |
</threat_model>

<verification>
- Verification script confirms 25 files, 75 unique abilities, correct types
- `uv run pytest tests/test_creatures.py -q` -- creature loading still works (run after Plan 01 completes)
</verification>

<success_criteria>
- All 25 creature JSON files have abilities arrays
- 75 unique ability names across the roster
- All ability types match creature types
- Damage multipliers follow tiered ranges
</success_criteria>

<output>
After completion, create `.planning/phases/06-battle-and-capture/06-02-SUMMARY.md`
</output>
