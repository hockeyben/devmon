---
phase: 06-battle-and-capture
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/devmon/engine/battle_engine.py
  - tests/test_battle.py
autonomous: true
requirements:
  - BATL-03
  - BATL-04
  - BATL-06
  - BATL-07
  - CAPT-02
  - CAPT-03
  - CAPT-04
  - CAPT-06
  - CREA-05

must_haves:
  truths:
    - "Damage formula uses ATK, DEF, level, type effectiveness, crit, and speed modifier"
    - "Type chart implements Fire > Nature > Water > Fire, Dark <> Light triangle"
    - "Critical hits occur at ~6% base rate with speed bonus, deal 1.5x damage"
    - "Capture chance = base_rate * (1 / hp_percent) * item_multiplier, clamped"
    - "Battle rewards grant player XP, creature XP, and currency"
    - "Creature XP and leveling computes stat growth"
    - "Faster creature acts first in turn order"
  artifacts:
    - path: "src/devmon/engine/battle_engine.py"
      provides: "Pure battle logic — damage, capture, rewards, turn order, AI"
      exports: ["compute_damage", "compute_capture_chance", "compute_battle_rewards", "get_type_effectiveness", "roll_crit", "determine_turn_order", "compute_max_hp", "get_available_abilities", "apply_creature_xp", "wild_creature_ai", "CAPTURE_ITEM_MULTIPLIERS", "TYPE_CHART"]
      min_lines: 150
  key_links:
    - from: "src/devmon/engine/battle_engine.py"
      to: "src/devmon/models/creature.py"
      via: "CreatureTemplate, OwnedCreature, Ability imports"
      pattern: "from devmon.models.creature import"
---

<objective>
Pure battle engine with all combat math, capture formula, rewards, and creature leveling.

Purpose: Build the domain logic layer for Phase 6. All functions are pure (no I/O, no Rich imports) and fully testable. The battle command (Plan 04) and render layer (Plan 03) will consume these functions.

Output: `engine/battle_engine.py` with compute_damage, capture chance, rewards, type chart, crit rolls, turn order, creature XP/leveling, and wild AI. xfail stubs in test_battle.py converted to real passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-battle-and-capture/06-CONTEXT.md
@.planning/phases/06-battle-and-capture/06-RESEARCH.md
@.planning/phases/06-battle-and-capture/06-01-SUMMARY.md

<interfaces>
From src/devmon/models/creature.py (after Plan 01):
```python
class Ability(BaseModel):
    name: str
    damage_multiplier: float = Field(gt=0.0)
    type: CreatureType
    learn_level: int = Field(ge=1)

class CreatureTemplate(BaseModel):
    id: str
    name: str
    type: CreatureType
    base_hp: int
    base_attack: int
    base_defense: int
    base_speed: int
    capture_rate: float
    abilities: list[Ability] = Field(default_factory=list)

class OwnedCreature(BaseModel):
    template_id: str
    level: int = 1
    xp: int = 0
    current_hp: Optional[int] = None
    is_fainted: bool = False
```

From src/devmon/models/encounter.py:
```python
class EncounterEntry(BaseModel):
    template_id: str
    encounter_level: int
    encounter_type: EncounterType  # "normal", "rare", "elite", "boss"
    rarity: str
    queued_at: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Battle engine pure functions — damage, type chart, crit, turn order, max HP</name>
  <files>src/devmon/engine/battle_engine.py, tests/test_battle.py</files>
  <read_first>
    src/devmon/models/creature.py,
    src/devmon/engine/progression.py,
    .planning/phases/06-battle-and-capture/06-CONTEXT.md,
    .planning/phases/06-battle-and-capture/06-RESEARCH.md,
    tests/test_battle.py
  </read_first>
  <behavior>
    - compute_damage(attacker_attack=20, attacker_level=5, attacker_speed=15, defender_defense=10, type_effectiveness=1.0, is_crit=False) returns positive int >= 1
    - compute_damage with type_effectiveness=1.5 returns roughly 1.5x the neutral result
    - compute_damage with is_crit=True returns roughly 1.5x the non-crit result
    - get_type_effectiveness("Fire", "Nature") returns 1.5
    - get_type_effectiveness("Fire", "Water") returns 0.5
    - get_type_effectiveness("Dark", "Light") returns 1.5
    - get_type_effectiveness("Fire", "Psychic") returns 1.0
    - roll_crit(speed=10) returns bool; over 1000 samples, crit rate is between 3% and 15%
    - determine_turn_order(speed_a=20, speed_b=10) returns "player" when player is faster
    - determine_turn_order(speed_a=10, speed_b=20) returns "wild" when wild is faster
    - compute_max_hp(template_with_base_hp_20, level=1) returns 20
    - compute_max_hp(template_with_base_hp_20, level=10) returns 38 (20 * (1 + 0.1 * 9))
  </behavior>
  <action>
Create `src/devmon/engine/battle_engine.py` as a pure domain module. NO imports from commands/, render/, or persistence/. Only imports from models/ and stdlib (random, math, dataclasses, enum).

**Architecture rule:** This module must NOT import Rich, Typer, or any I/O library.

Implement per D-07, D-08, D-09, D-01:

```python
"""Battle engine — pure domain logic for combat, capture, rewards.

No I/O. No Rich. No Typer. No persistence imports.
Only imports from models/ and stdlib.
"""
from __future__ import annotations
import enum
import math
import random
from dataclasses import dataclass, field
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from devmon.models.creature import Ability, CreatureTemplate, OwnedCreature
```

**TYPE_CHART** (D-08):
```python
TYPE_CHART: dict[str, dict[str, float]] = {
    "Fire":    {"Nature": 1.5, "Water": 0.5},
    "Water":   {"Fire": 1.5, "Nature": 0.5},
    "Nature":  {"Water": 1.5, "Fire": 0.5},
    "Dark":    {"Light": 1.5},
    "Light":   {"Dark": 1.5},
}
```

**get_type_effectiveness(attacker_type, defender_type) -> float**: Lookup in TYPE_CHART, default 1.0.

**compute_max_hp(template, level) -> int**: `int(template.base_hp * (1 + 0.1 * (level - 1)))`. Minimum 1.

**compute_stat(base_stat, level) -> int**: `int(base_stat * (1 + 0.05 * (level - 1)))`. Used for ATK, DEF, SPD scaling. Minimum 1.

**compute_damage(attacker_attack, attacker_level, attacker_speed, defender_defense, type_effectiveness, is_crit) -> int**: Formula per D-07: `base = ((2 * level / 5 + 2) * atk / max(1, def)) / 50 + 2`, then `* type_effectiveness * speed_mod * crit_mult * random.uniform(0.9, 1.1)`. Minimum 1.

**roll_crit(attacker_speed) -> bool**: `random.random() < min(0.06 + attacker_speed * 0.001, 0.15)` per D-09.

**determine_turn_order(player_speed, wild_speed) -> str**: Returns "player" if player_speed >= wild_speed, else "wild". Per D-01 faster acts first; tie goes to player.

Convert xfail stubs for BATL-03, BATL-04 in test_battle.py to real passing tests. Remove the `xfail` decorators from those tests and write real assertions against the functions above.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_battle.py::test_faster_creature_acts_first tests/test_battle.py::test_damage_uses_atk_def_type_effectiveness -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def compute_damage" src/devmon/engine/battle_engine.py returns match
    - grep "def get_type_effectiveness" src/devmon/engine/battle_engine.py returns match
    - grep "TYPE_CHART" src/devmon/engine/battle_engine.py returns match
    - grep "def roll_crit" src/devmon/engine/battle_engine.py returns match
    - grep "def determine_turn_order" src/devmon/engine/battle_engine.py returns match
    - grep "def compute_max_hp" src/devmon/engine/battle_engine.py returns match
    - grep -c "import rich\|from rich" src/devmon/engine/battle_engine.py returns 0 (no Rich imports)
    - Tests test_faster_creature_acts_first and test_damage_uses_atk_def_type_effectiveness pass (not xfail)
  </acceptance_criteria>
  <done>Battle engine has compute_damage, type chart, crit roll, turn order, max_hp. All pure functions. BATL-03 and BATL-04 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture formula, battle rewards, creature XP/leveling, wild AI, abilities</name>
  <files>src/devmon/engine/battle_engine.py, tests/test_battle.py</files>
  <read_first>
    src/devmon/engine/battle_engine.py,
    tests/test_battle.py,
    .planning/phases/06-battle-and-capture/06-CONTEXT.md
  </read_first>
  <behavior>
    - compute_capture_chance(base_rate=0.7, hp_percent=1.0, item_multiplier=1.0) returns approximately 0.7
    - compute_capture_chance(base_rate=0.7, hp_percent=0.5, item_multiplier=1.0) returns approximately 1.0 (clamped)
    - compute_capture_chance(base_rate=0.3, hp_percent=0.1, item_multiplier=1.0) returns 1.0 (clamped at max 1.0)
    - compute_capture_chance(base_rate=0.3, hp_percent=0.5, item_multiplier=1.5) returns 0.9
    - compute_capture_chance with hp_percent=0 clamps to 0.01 minimum (no division by zero)
    - CAPTURE_ITEM_MULTIPLIERS["basic"] == 1.0
    - CAPTURE_ITEM_MULTIPLIERS["great"] == 1.5
    - CAPTURE_ITEM_MULTIPLIERS["ultra"] == 2.0
    - CAPTURE_ITEM_MULTIPLIERS["master"] == 100.0
    - compute_battle_rewards(wild_level=5, encounter_type="normal") returns dict with player_xp > 0, creature_xp > 0, currency > 0
    - compute_battle_rewards(wild_level=5, encounter_type="boss") returns higher rewards than "normal"
    - apply_creature_xp(owned, template, xp=100) updates owned.xp, potentially bumps owned.level
    - get_available_abilities(abilities_list, creature_level=3) returns only abilities with learn_level <= 3
    - wild_creature_ai(available_abilities, has_abilities=True) returns either "attack" or an ability
  </behavior>
  <action>
Add to `src/devmon/engine/battle_engine.py`:

**CAPTURE_ITEM_MULTIPLIERS** (D-13):
```python
CAPTURE_ITEM_MULTIPLIERS: dict[str, float] = {
    "basic": 1.0,
    "great": 1.5,
    "ultra": 2.0,
    "master": 100.0,
}
```

**compute_capture_chance(base_rate, hp_percent, item_multiplier=1.0) -> float** (D-11):
`chance = base_rate * (1 / max(0.01, hp_percent)) * item_multiplier`. Clamp result to [0.0, 1.0]. Guard against division by zero by clamping hp_percent to minimum 0.01.

**attempt_capture(capture_chance) -> bool**: `random.random() < capture_chance`.

**compute_battle_rewards(wild_level, encounter_type) -> dict** (BATL-06):
Returns `{"player_xp": int, "creature_xp": int, "currency": int}`.
Base formula: `player_xp = 20 + wild_level * 5`, `creature_xp = 15 + wild_level * 4`, `currency = 10 + wild_level * 3`.
Encounter type multipliers: normal=1.0, rare=1.5, elite=2.0, boss=3.0.

**compute_capture_rewards(wild_level, rarity) -> dict** (CAPT-05):
Returns `{"player_xp": int, "currency": int}`.
Capture bonus: `player_xp = 30 + wild_level * 8`, `currency = 15 + wild_level * 5`.
Rarity multipliers: common=1.0, uncommon=1.2, rare=1.5, epic=2.0, legendary=3.0.

**apply_creature_xp(owned, template, xp_gained) -> bool** (CREA-05):
Add xp_gained to owned.xp. Check level-up: creature levels up when `xp >= level * 50` (simple curve). On level-up: increment owned.level, reset owned.xp to remainder, recalculate current_hp proportionally. Return True if leveled up, False otherwise.

**apply_faint(owned) -> None** (BATL-07):
Set `owned.current_hp = 0` and `owned.is_fainted = True`.

**get_available_abilities(abilities, creature_level) -> list[Ability]** (CREA-06):
Return `[a for a in abilities if a.learn_level <= creature_level]`.

**wild_creature_ai(available_abilities) -> str** (Claude's discretion):
If available_abilities is non-empty: 40% chance to use a random ability (return ability name), 60% chance normal attack (return "attack"). If no abilities: always return "attack".

**resolve_wild_flee_after_failed_capture() -> bool** (D-14):
15% chance: `random.random() < 0.15`.

Convert xfail stubs for BATL-06, BATL-07, CAPT-02, CAPT-03, CAPT-04, CAPT-05, CAPT-06, CAPT-07, CREA-05, CREA-06 in test_battle.py to real passing tests.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_battle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def compute_capture_chance" src/devmon/engine/battle_engine.py returns match
    - grep "CAPTURE_ITEM_MULTIPLIERS" src/devmon/engine/battle_engine.py returns match
    - grep "def compute_battle_rewards" src/devmon/engine/battle_engine.py returns match
    - grep "def apply_creature_xp" src/devmon/engine/battle_engine.py returns match
    - grep "def get_available_abilities" src/devmon/engine/battle_engine.py returns match
    - grep "def wild_creature_ai" src/devmon/engine/battle_engine.py returns match
    - grep "def apply_faint" src/devmon/engine/battle_engine.py returns match
    - grep -c "xfail" tests/test_battle.py returns at most 4 (only BATL-01, BATL-02, BATL-05, BATL-08 remain xfail)
    - uv run pytest tests/test_battle.py -q passes with 0 failures
  </acceptance_criteria>
  <done>Capture formula, rewards, creature XP/leveling, abilities, wild AI, and faint logic all implemented as pure functions. 14 of 18 test stubs now pass as real tests. Only UI/CLI stubs remain xfail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Creature JSON abilities -> battle engine | Ability damage_multiplier values affect combat balance |
| RNG seed -> combat outcomes | random module used for crit, capture, AI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T (Tampering) | creature abilities | accept | Single-player local game; user can edit their own JSON |
| T-06-04 | D (Denial of Service) | compute_damage division | mitigate | max(1, defender_defense) prevents division by zero |
| T-06-05 | D (Denial of Service) | capture formula | mitigate | max(0.01, hp_percent) prevents ZeroDivisionError |
</threat_model>

<verification>
- `uv run pytest tests/test_battle.py -q` — all non-xfail tests pass
- `uv run pytest tests/ -q` — full suite green
- No Rich imports in battle_engine.py
</verification>

<success_criteria>
- battle_engine.py is pure domain logic: no I/O, no Rich, no Typer
- compute_damage, get_type_effectiveness, roll_crit, determine_turn_order work correctly
- compute_capture_chance follows D-11 formula with division-by-zero guard
- Battle and capture rewards computed with encounter type multipliers
- Creature XP/leveling with stat growth functional
- get_available_abilities filters by level correctly
- 14+ tests passing as real assertions (not xfail)
</success_criteria>

<output>
After completion, create `.planning/phases/06-battle-and-capture/06-02-SUMMARY.md`
</output>
