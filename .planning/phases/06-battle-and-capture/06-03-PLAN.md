---
phase: 06-battle-and-capture
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/devmon/render/battle.py
  - tests/test_battle.py
autonomous: true
requirements:
  - BATL-05
  - UI-03

must_haves:
  truths:
    - "HP bar renders with green/yellow/red color thresholds at 50%/25%"
    - "Battle creature panel shows ASCII art, HP bar, level, and type"
    - "Capture animation prints 3 shake lines with pauses"
    - "Victory, capture, defeat, and flee result screens render correctly"
    - "Action menu renders all 6 options with correct styling"
  artifacts:
    - path: "src/devmon/render/battle.py"
      provides: "All battle rendering — HP bars, panels, animation, results, action menu"
      exports: ["render_hp_bar", "render_battle_creature_panel", "build_battle_renderable", "run_capture_animation", "render_victory_screen", "render_capture_screen", "render_defeat_screen", "render_flee_message", "render_action_menu"]
      min_lines: 150
  key_links:
    - from: "src/devmon/render/battle.py"
      to: "src/devmon/render/themes.py"
      via: "RARITY_COLORS import for panel border colors"
      pattern: "from devmon.render.themes import"
    - from: "src/devmon/render/battle.py"
      to: "src/devmon/models/creature.py"
      via: "CreatureTemplate import for panel rendering"
      pattern: "from devmon.models.creature import"
---

<objective>
Battle rendering layer — HP bars, battle panels, capture animation, result screens, and action menu.

Purpose: Build all Rich rendering for the battle screen as pure render functions (no game logic, no persistence). These are consumed by the battle command (Plan 04) which orchestrates the battle loop.

Output: `render/battle.py` with render_hp_bar, render_battle_creature_panel, build_battle_renderable, run_capture_animation, render_victory/capture/defeat/flee screens, render_action_menu. BATL-05 and UI-03 xfail tests converted to real tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-battle-and-capture/06-CONTEXT.md
@.planning/phases/06-battle-and-capture/06-UI-SPEC.md
@.planning/phases/06-battle-and-capture/06-02-SUMMARY.md

<interfaces>
From src/devmon/models/creature.py:
```python
class CreatureTemplate(BaseModel):
    id: str
    name: str
    type: CreatureType
    rarity: CreatureRarity
    base_hp: int
    ascii_art: list[str]
    primary_color: str
    abilities: list[Ability]

class Ability(BaseModel):
    name: str
    damage_multiplier: float
    type: CreatureType
    learn_level: int
```

From src/devmon/render/themes.py:
```python
RARITY_COLORS: dict[str, str] = {
    "common": "white",
    "uncommon": "green",
    "rare": "bright_blue",
    "epic": "magenta",
    "legendary": "bold yellow",
}
def get_theme(name: str) -> dict[str, str]: ...
```

From src/devmon/render/creatures.py:
```python
def render_creature_panel(template, console, theme, ...): ...
# DO NOT reuse for battle — use compact render_battle_creature_panel instead
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HP bar, battle creature panel, and action menu rendering</name>
  <files>src/devmon/render/battle.py, tests/test_battle.py</files>
  <read_first>
    src/devmon/render/creatures.py,
    src/devmon/render/themes.py,
    .planning/phases/06-battle-and-capture/06-UI-SPEC.md,
    tests/test_battle.py
  </read_first>
  <action>
Create `src/devmon/render/battle.py`. This module is pure render — no game logic, no persistence, no engine imports.

**Architecture rule:** Import only from models/ and render/ (themes). Never import from engine/, commands/, or persistence/.

**render_hp_bar(current, max_hp, width=20) -> Text** (D-17, UI-SPEC HP Bar Render):
```python
from rich.text import Text

def render_hp_bar(current: int, max_hp: int, width: int = 20) -> Text:
    hp_percent = current / max_hp if max_hp > 0 else 0
    filled = round(hp_percent * width)
    empty = width - filled
    color = "green" if hp_percent > 0.50 else ("yellow" if hp_percent > 0.25 else "red")
    bar = Text()
    bar.append("HP ", style="dim cyan")
    bar.append("\u2588" * filled, style=color)
    bar.append("\u2591" * empty, style="dim white")
    bar.append(f" {current}/{max_hp}", style=color)
    return bar
```

**render_battle_creature_panel(template, current_hp, max_hp, level, prefix, rarity) -> Panel** (UI-SPEC Surface 1):
- Panel with `box.ROUNDED`, `expand=False`, `border_style=RARITY_COLORS[rarity]`
- Title: `"{prefix}: {template.name}"` styled `bold {RARITY_COLORS[rarity]}`
- Body: ASCII art lines in template.primary_color, blank line, HP bar, stat row `"LVL {level}  Type {template.type}"`
- Do NOT show flavor text, capture rate, or full stat block (compact battle panel per UI-SPEC)

**build_battle_renderable(wild_panel, player_panel, turn_number, last_narration, action_menu_text) -> Group** (UI-SPEC Surface 1):
Returns a Rich `Group` containing: wild panel, blank line, player panel, blank line, turn narration line, blank line, action menu text.
Turn narration: `"Turn {N} \u2014 {last_narration}"` with turn number in `dim white`.

**render_action_menu(ability_name, can_switch, turn_number) -> Text** (UI-SPEC Surface 1):
Returns Rich Text block with the 6-item action menu per UI-SPEC:
```
  Your turn! What will you do?

  [1] Attack
  [2] Special Ability  ({ability_name})
  [3] Capture
  [4] Switch Creature
  [5] Items  (coming soon)
  [6] Flee
```
- ability_name: if None, show "(none yet)" in dim white
- can_switch: if False, show "[4] Switch Creature  (no other party members)" in dim white
- "[5] Items  (coming soon)" always dim white
- All others in white, heading in bold white
- 2-space indent on all lines

Convert BATL-05 xfail stub in test_battle.py to a real test that:
- Creates a mock CreatureTemplate
- Calls render_hp_bar(45, 80) and asserts the return is a Rich Text object
- Calls render_battle_creature_panel with test data and asserts it returns a Panel
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_battle.py::test_battle_screen_renders_hp_bars_and_art -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def render_hp_bar" src/devmon/render/battle.py returns match
    - grep "def render_battle_creature_panel" src/devmon/render/battle.py returns match
    - grep "def build_battle_renderable" src/devmon/render/battle.py returns match
    - grep "def render_action_menu" src/devmon/render/battle.py returns match
    - grep -c "import.*battle_engine\|from.*engine" src/devmon/render/battle.py returns 0 (no engine imports)
    - test_battle_screen_renders_hp_bars_and_art passes (not xfail)
  </acceptance_criteria>
  <done>HP bar, battle creature panel, action menu, and build_battle_renderable all implemented. BATL-05 test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Capture animation, result screens, flee message rendering</name>
  <files>src/devmon/render/battle.py, tests/test_battle.py</files>
  <read_first>
    src/devmon/render/battle.py,
    .planning/phases/06-battle-and-capture/06-UI-SPEC.md,
    tests/test_battle.py
  </read_first>
  <action>
Add to `src/devmon/render/battle.py`:

**run_capture_animation(console, item_name, creature_name, rarity, success) -> None** (UI-SPEC Surface 3, D-12):
Prints the capture shake sequence to console with `time.sleep(0.6)` between lines:
```
You threw a {item_name}!

  The capsule wobbles...
  It shakes again...
  One more shake...
```
Then outcome:
- Success: `"  \u2728 CLICK! {creature_name} was captured! \u2728"` styled `bold {RARITY_COLORS[rarity]}`
- Failure: `"  {creature_name} broke free!"` styled `bold red`

Import `time` for sleep. The console parameter is a Rich Console instance.

**render_victory_screen(console, player_creature_name, wild_name, rewards) -> None** (UI-SPEC Surface 4 — Win):
Print a Rich Panel with `box.ROUNDED`, `expand=False`, `border_style="bold yellow"`, title `"Victory!"`.
Below panel: `"{player_creature_name} defeated {wild_name}!"`, then rewards section:
```
  Rewards:
    Player XP:   +{rewards['player_xp']}
    {player_creature_name}:  +{rewards['creature_xp']} XP
    Currency:    +{rewards['currency']} bits
```
Label in `dim white`, value in `bold white`. Then `"Press Enter to continue."`.

**render_capture_screen(console, creature_name, rarity, rewards) -> None** (UI-SPEC Surface 4 — Capture):
Panel with `border_style=RARITY_COLORS[rarity]`, title `"{creature_name} Captured!"` in `bold {rarity_color}`.
Body: `"{creature_name} was added to your collection."`, rewards section with capture bonus XP and currency.

**render_defeat_screen(console) -> None** (UI-SPEC Surface 4 — Defeat):
Panel with `border_style="dim red"`, title `"Defeated..."` in `bold red`.
Body: `"Your party was wiped out. No rewards this time.\nYour creatures need rest before their next battle."` in `dim white`.

**render_flee_message(console, wild_name, rarity) -> None** (UI-SPEC Surface 4 — Flee):
Single line: `"You fled from {wild_name}. Encounter lost."` with wild_name in `bold {RARITY_COLORS[rarity]}`.
No panel, no "Press Enter" prompt.

**render_wild_fled_message(console, wild_name, rarity) -> None** (D-14):
Single line: `"{wild_name} fled!"` with wild_name in `bold {RARITY_COLORS[rarity]}`.

**render_faint_message(console, creature_name, is_player) -> None** (UI-SPEC Surface 5):
If is_player: `"{creature_name} fainted! Switching to next party member..."` with faint part in `bold red` and switch part in `dim white`.
If not is_player (wild): `"{creature_name} fainted!"` in `bold red`.

Convert UI-03 remaining xfail stub if any. Add a test for render_victory_screen that verifies it creates output without error (capture console output with Console(record=True)).
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_battle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def run_capture_animation" src/devmon/render/battle.py returns match
    - grep "def render_victory_screen" src/devmon/render/battle.py returns match
    - grep "def render_capture_screen" src/devmon/render/battle.py returns match
    - grep "def render_defeat_screen" src/devmon/render/battle.py returns match
    - grep "def render_flee_message" src/devmon/render/battle.py returns match
    - grep "time.sleep" src/devmon/render/battle.py returns match
    - uv run pytest tests/test_battle.py -q passes with 0 failures
  </acceptance_criteria>
  <done>All battle result screens, capture animation, faint/flee messages implemented. All render functions are pure render (no game logic). UI-03 test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render input data -> Rich output | Creature names and stats rendered to terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | I (Information Disclosure) | capture rate | mitigate | render functions never display capture_rate value (D-15); only show capture outcome |
| T-06-07 | D (Denial of Service) | HP bar division | mitigate | max_hp guard: hp_percent = current / max_hp if max_hp > 0 else 0 |
</threat_model>

<verification>
- `uv run pytest tests/test_battle.py -q` — all non-xfail tests pass
- `uv run pytest tests/ -q` — full suite green
- No engine imports in render/battle.py
</verification>

<success_criteria>
- render/battle.py is pure render: no engine imports, no persistence imports
- HP bar color thresholds: green >50%, yellow 25-50%, red <=25%
- Battle creature panel shows compact view (art + HP + level/type only)
- Capture animation has 3 shake lines with time.sleep(0.6) pauses
- All 4 result screens (victory, capture, defeat, flee) match UI-SPEC exactly
- Action menu has all 6 items with correct dim/active styling
- BATL-05 and UI-03 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-battle-and-capture/06-03-SUMMARY.md`
</output>
