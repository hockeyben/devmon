---
phase: 999.1-improve-ascii-art-for-all-25-creatures
plan: 03
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - src/devmon/data/creatures/boulder_bash.json
  - src/devmon/data/creatures/char_mander.json
  - src/devmon/data/creatures/cyber_beetle.json
  - src/devmon/data/creatures/mind_moth.json
  - src/devmon/data/creatures/nullhound.json
  - src/devmon/data/creatures/stackcat.json
  - src/devmon/data/creatures/wave_runner.json
  - src/devmon/data/creatures/zap_ferret.json
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
  - ART-06

must_haves:
  truths:
    - "All 8 uncommon creatures have full-body detailed ASCII art with 8-10 lines (D-01, D-04)"
    - "Art uses realistic proportions with ASCII shading (D-02)"
    - "Aggressive creatures have action poses, gentle ones calm poses (D-03)"
    - "Rich markup tags provide per-character coloring (D-05)"
    - "All art stays within 20 visual columns wide (D-04)"
  artifacts:
    - path: "src/devmon/data/creatures/boulder_bash.json"
      provides: "Upgraded BoulderBash art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/char_mander.json"
      provides: "Upgraded CharMander art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/cyber_beetle.json"
      provides: "Upgraded CyberBeetle art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/mind_moth.json"
      provides: "Upgraded MindMoth art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/nullhound.json"
      provides: "Upgraded Nullhound art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/stackcat.json"
      provides: "Upgraded Stackcat art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/wave_runner.json"
      provides: "Upgraded WaveRunner art"
      contains: "ascii_art"
    - path: "src/devmon/data/creatures/zap_ferret.json"
      provides: "Upgraded ZapFerret art"
      contains: "ascii_art"
  key_links:
    - from: "src/devmon/data/creatures/*.json"
      to: "src/devmon/models/creature.py"
      via: "Pydantic validation on load"
      pattern: "_validate_ascii_art"
---

<objective>
Create detailed, full-body ASCII art with Rich markup coloring for all 8 uncommon-tier creatures. Second batch per D-06 rarity tier ordering.

Purpose: Uncommon creatures are mid-evolution forms for many chains (cyber_beetle from bugbyte, nullhound from gloom_bat, etc.). Their art should show growth and increased detail compared to common forms while staying in the 8-10 line range.

Output: 8 upgraded creature JSON files with new ascii_art arrays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-RESEARCH.md
@.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-01-SUMMARY.md

<interfaces>
<!-- Creature identity reference for art design -->

| Creature | Species | Type | Personality | Evolves From | Evolves To |
|----------|---------|------|-------------|-------------|------------|
| boulder_bash | (check JSON) | Earth | Aggressive basher | -- | moss_golem |
| char_mander | (check JSON) | Fire | Standalone fire lizard | -- | -- |
| cyber_beetle | (check JSON) | Psychic/Tech | Evolved from bugbyte | bugbyte | -- |
| mind_moth | (check JSON) | Psychic | Evolved from hex_owl | hex_owl | -- |
| nullhound | (check JSON) | Shadow | Evolved from gloom_bat, aggressive | gloom_bat | -- |
| stackcat | (check JSON) | Digital | Tech cat, curious | -- | kraken_byte |
| wave_runner | (check JSON) | Water | Swift swimmer | -- | tide_byte |
| zap_ferret | (check JSON) | Electric | Quick, evolved into volt_whisker | -- | volt_whisker |

Art constraints (D-04): Uncommon = 8-10 lines, max 20 visual cols wide.
D-05: Use Rich markup for coloring.
D-08: Evolved forms (cyber_beetle, mind_moth, nullhound) should show more detail/menace than their base forms.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ASCII art for boulder_bash, char_mander, cyber_beetle, mind_moth</name>
  <files>src/devmon/data/creatures/boulder_bash.json, src/devmon/data/creatures/char_mander.json, src/devmon/data/creatures/cyber_beetle.json, src/devmon/data/creatures/mind_moth.json</files>
  <read_first>src/devmon/data/creatures/boulder_bash.json, src/devmon/data/creatures/char_mander.json, src/devmon/data/creatures/cyber_beetle.json, src/devmon/data/creatures/mind_moth.json</read_first>
  <action>
    Replace `ascii_art` arrays with 8-10 line, max 20 visual column designs using Rich markup.

    **Design guidelines:**

    1. **boulder_bash** (Earth type): Heavy rock creature with craggy body, thick arms ready to smash, aggressive charging pose (D-03). Use `[dim yellow]` for rock, `[bold yellow]` for cracks/impact lines, `[dim white]` for dust. IMPORTANT: escape any literal `[` or `]` in existing art as `\[` and `\]` — research notes existing art has `[##] [##]`.

    2. **char_mander** (Fire type): Fire salamander/lizard with flame tail, scales, four legs, alert pose. Standalone creature (no evolution chain) — give it a distinctive, complete design. Use `[bold red]` for flames, `[yellow]` for body, `[bright_red]` for belly.

    3. **cyber_beetle** (Psychic/Tech type): Evolved form of bugbyte — larger, armored digital beetle with circuit-pattern shell, mandibles, six legs. More imposing than bugbyte (D-08). Use `[bold magenta]` for shell, `[bright_cyan]` for circuit patterns, `[bright_magenta]` for eyes.

    4. **mind_moth** (Psychic type): Evolved from hex_owl — large moth with psychic eye patterns on wings, antennae, floating pose. Use `[bold magenta]` for wings, `[bright_yellow]` for eye patterns, `[dim magenta]` for body.

    **Rules:** Escape `[`/`]`, test each loads, visual width <= 20, full body, realistic proportions.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_creatures.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each JSON has ascii_art with 8-10 lines
    - uv run python -c "from devmon.data.creature_loader import load_all_creatures; load_all_creatures()" exits 0
    - No line exceeds 20 visual columns after markup stripped
    - uv run pytest tests/test_creatures.py -x passes
  </acceptance_criteria>
  <done>4 uncommon creatures have detailed art with Rich markup coloring. All pass validation.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ASCII art for nullhound, stackcat, wave_runner, zap_ferret</name>
  <files>src/devmon/data/creatures/nullhound.json, src/devmon/data/creatures/stackcat.json, src/devmon/data/creatures/wave_runner.json, src/devmon/data/creatures/zap_ferret.json</files>
  <read_first>src/devmon/data/creatures/nullhound.json, src/devmon/data/creatures/stackcat.json, src/devmon/data/creatures/wave_runner.json, src/devmon/data/creatures/zap_ferret.json</read_first>
  <action>
    Replace `ascii_art` arrays with 8-10 line, max 20 visual column designs using Rich markup.

    **Design guidelines:**

    5. **nullhound** (Shadow type): Evolved from gloom_bat — shadow hound with void-dark fur, glowing eyes, snarling attack pose (D-03 aggressive). Use `[dim magenta]` for shadow body, `[bold red]` for eyes, `[bright_magenta]` for void energy.

    6. **stackcat** (Digital type): Tech-themed cat with data patterns in fur, alert curious pose, tail with data stream. Use `[bold cyan]` for data patterns, `[white]` for body, `[bright_cyan]` for eyes.

    7. **wave_runner** (Water type): Swift aquatic creature with fins, streamlined body, swimming/leaping pose (energetic). Use `[bold blue]` for body, `[bright_cyan]` for water spray, `[dim blue]` for depth.

    8. **zap_ferret** (Electric type): Electric ferret with crackling fur, long body, playful/darting pose. Use `[bold yellow]` for sparks, `[yellow]` for body, `[bright_yellow]` for crackling effects.

    **Rules:** Escape `[`/`]`, test each loads, visual width <= 20, full body, realistic proportions, personality-matching poses.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_creatures.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each JSON has ascii_art with 8-10 lines
    - uv run python -c "from devmon.data.creature_loader import load_all_creatures; load_all_creatures()" exits 0
    - No line exceeds 20 visual columns after markup stripped
    - uv run pytest tests/test_creatures.py -x passes
  </acceptance_criteria>
  <done>All 8 uncommon creatures have detailed, full-body art with Rich markup coloring. Uncommon tier complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON data -> Pydantic model | Art strings validated by _validate_ascii_art |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | T (Tampering) | ascii_art JSON content | accept | Local data files, not user input. |
</threat_model>

<verification>
- All 8 uncommon creature JSONs load without validation errors
- `uv run pytest tests/test_creatures.py -x` passes
- `uv run pytest` full suite passes
</verification>

<success_criteria>
- 8 uncommon creatures have 8-10 line detailed art with Rich markup coloring
- Evolved forms (cyber_beetle, mind_moth, nullhound) are visibly more detailed than their base forms
- All art lines <= 20 visual columns wide
- All pass Pydantic validation
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-03-SUMMARY.md`
</output>
