---
phase: 999.1-improve-ascii-art-for-all-25-creatures
plan: 05
type: execute
wave: 4
depends_on: ["999.1-04"]
files_modified:
  - src/devmon/render/evolution.py
  - src/devmon/render/creatures.py
  - tests/test_evolution.py
autonomous: false
requirements:
  - ART-07
  - ART-08

must_haves:
  truths:
    - "Evolution display shows old and new creature art side-by-side in wide terminals (D-07)"
    - "Evolution display falls back to vertical stacking in narrow terminals (< 40 cols)"
    - "Side-by-side panels handle height mismatches gracefully"
    - "Human has visually verified art quality across all rarity tiers"
  artifacts:
    - path: "src/devmon/render/evolution.py"
      provides: "Side-by-side evolution before/after display"
      contains: "Table.grid"
    - path: "src/devmon/render/creatures.py"
      provides: "Panel-building helper for reuse"
      contains: "_build_creature_panel"
    - path: "tests/test_evolution.py"
      provides: "Tests for side-by-side evolution display"
      contains: "side_by_side"
  key_links:
    - from: "src/devmon/render/evolution.py"
      to: "src/devmon/render/creatures.py"
      via: "_build_creature_panel helper import"
      pattern: "_build_creature_panel"
    - from: "src/devmon/render/evolution.py"
      to: "rich.table.Table"
      via: "Table.grid for side-by-side layout"
      pattern: "Table\\.grid"
---

<objective>
Implement side-by-side evolution before/after display (D-07) and conduct human visual verification of all creature art across the game.

Purpose: D-07 requires showing old creature art next to new evolved form during evolution, not sequential vertical stacking. This is the final code change for the phase. The human checkpoint verifies all 26 creatures look good in battle, collection, and evolution contexts.

Output: Updated evolution renderer with side-by-side display, _build_creature_panel helper, human approval of art quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-RESEARCH.md
@.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-04-SUMMARY.md

<interfaces>
From src/devmon/render/creatures.py:
```python
def render_creature_panel(
    template: CreatureTemplate,
    console: Console,
    theme: dict[str, str] | None = None,
    encounter_level: int | None = None,
    encounter_type: str | None = None,
    encounter_rarity: str | None = None,
    narrow: bool = False,
) -> None:
    # Currently prints directly to console, does NOT return Panel
```

From src/devmon/render/evolution.py:
```python
def render_evolution_before_after(
    old_template: "CreatureTemplate",
    new_template: "CreatureTemplate",
    console: Console,
    narrow: bool = False,
) -> None:
    # Currently: vertical stacking (panel, arrow, panel)
    # Required: side-by-side Table.grid layout (D-07)
```

Research recommendation: Add `_build_creature_panel()` helper that returns Panel object.
`render_creature_panel` calls the helper and prints. Evolution renderer imports the helper.
No breaking change to existing callers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement side-by-side evolution display with _build_creature_panel helper</name>
  <files>src/devmon/render/creatures.py, src/devmon/render/evolution.py, tests/test_evolution.py</files>
  <read_first>src/devmon/render/creatures.py, src/devmon/render/evolution.py, tests/test_evolution.py</read_first>
  <behavior>
    - Test: render_evolution_before_after produces Rich Table output (not just two sequential panels)
    - Test: In narrow mode (narrow=True), falls back to vertical stacking (no Table)
    - Test: _build_creature_panel returns a Panel object (not None)
    - Test: render_creature_panel still works identically for existing callers (no regression)
  </behavior>
  <action>
    1. In `src/devmon/render/creatures.py`:
       - Extract the Panel-building logic from `render_creature_panel` into a new function `_build_creature_panel` with the same parameters except `console`. It returns `Panel` instead of printing.
       - Update `render_creature_panel` to call `_build_creature_panel` and then `console.print(panel)`.
       - This is a refactor — no behavior change for any existing caller.

    2. In `src/devmon/render/evolution.py`:
       - Add imports: `from rich.table import Table` and `from devmon.render.creatures import _build_creature_panel`
       - Rewrite `render_evolution_before_after`:
         - **Wide mode (narrow=False):** Create a `Table.grid(expand=False, padding=(0, 2))` with two columns. Build Panel for old_template and Panel for new_template using `_build_creature_panel`. Add a single row with both panels. Print the table. Print the evolution message below.
         - **Narrow mode (narrow=True):** Keep the existing vertical stacking behavior (render old panel, arrow, new panel).
       - The _build_creature_panel call for evolution display should pass minimal params (no encounter_level, no encounter_type) — just template and optional theme.

    3. In `tests/test_evolution.py`:
       - Add `test_evolution_side_by_side_wide`: Use `Console(record=True)` to capture output from `render_evolution_before_after(old, new, console, narrow=False)`. Assert the output does NOT contain the vertical arrow "Evolving..." between two separate panels, and DOES render both creatures.
       - Add `test_evolution_narrow_fallback`: Same but with `narrow=True`. Assert it falls back to sequential rendering.

    **IMPORTANT:** The `_build_creature_panel` function name starts with underscore (internal helper) but is imported cross-module. This is acceptable since both modules are in the same `render/` package.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest tests/test_evolution.py tests/test_creatures.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "_build_creature_panel" src/devmon/render/creatures.py returns 2+ (definition + call)
    - grep -c "Table.grid" src/devmon/render/evolution.py returns 1+
    - grep -c "_build_creature_panel" src/devmon/render/evolution.py returns 1+
    - grep -c "side_by_side" tests/test_evolution.py returns 1+
    - uv run pytest tests/test_evolution.py -x passes
    - uv run pytest -x passes (full suite, no regression)
  </acceptance_criteria>
  <done>Evolution before/after renders side-by-side in wide mode via Table.grid, falls back to vertical in narrow mode. _build_creature_panel helper extracted. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all creature art and evolution display</name>
  <files>none</files>
  <read_first>src/devmon/render/evolution.py, src/devmon/render/creatures.py</read_first>
  <action>
    Present the human with verification steps. All automated work is complete — this is visual quality approval only.

    All 26 creatures have upgraded ASCII art with Rich markup coloring:
    - 8 common creatures (8-10 lines)
    - 8 uncommon creatures (8-10 lines)
    - 5 rare creatures (10-14 lines)
    - 3 epic creatures (14-18 lines)
    - 2 legendary creatures (14-18 lines)

    Plus: Side-by-side evolution before/after display.
  </action>
  <verify>
    <automated>cd "C:/Users/flopp/OneDrive/Desktop/Devmon" && uv run pytest -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms art looks detailed, colored, and proportional across all rarity tiers
    - No literal markup tags visible in any creature display
    - Evolution side-by-side display works in wide terminal
    - No layout overflow in 80-column terminal
  </acceptance_criteria>
  <how-to-verify>
    1. **View all creatures in collection:**
       Run `uv run devmon collection` — scroll through and check that art looks detailed, colored, and proportional. Verify rarity tiers have increasing visual complexity.

    2. **Battle a creature:**
       Run `uv run devmon battle` — verify creature art renders correctly in battle panels with HP bars, level, and markup colors.

    3. **Check a specific creature detail:**
       Run `uv run devmon collection detail <creature_name>` for a few creatures across tiers — verify full-body art with limbs, features, and coloring.

    4. **Trigger an evolution (if possible):**
       If a creature is close to evolution level, battle to trigger it. Verify the before/after display shows old and new art SIDE-BY-SIDE (not stacked vertically). If no evolution is ready, check the test output from test_evolution.py instead.

    5. **Narrow terminal test:**
       Resize terminal to ~35 columns. Run `uv run devmon collection` — verify art is skipped gracefully in narrow mode (existing behavior).

    **Expected:** Art is detailed and recognizable. Colors render correctly. No literal [bold red] text visible. No layout overflow. Evolution side-by-side works in wide terminal.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific creatures/views that need art revision.</resume-signal>
  <done>Human approves all creature art quality and evolution side-by-side display. Phase 999.1 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render output -> terminal | Rich rendering to terminal display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-06 | D (DoS) | Table.grid with large art panels | accept | Art is capped at 18 lines x 20 cols by validator. Two panels side-by-side fits any 80-col terminal. |
</threat_model>

<verification>
- `uv run pytest tests/test_evolution.py -x` passes
- `uv run pytest` full suite passes
- Human approves art quality across all tiers
</verification>

<success_criteria>
- Evolution renders side-by-side in wide terminals
- Evolution falls back to vertical in narrow terminals
- Human confirms all 26 creatures look good
- No literal markup tags visible anywhere
- No layout overflow in 80-col terminal
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-improve-ascii-art-for-all-25-creatures/999.1-05-SUMMARY.md`
</output>
