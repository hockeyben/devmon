---
phase: 03-player-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_status.py
  - tests/test_prompt.py
  - tests/test_settings.py
  - tests/test_themes.py
  - tests/test_models.py
  - tests/test_persistence.py
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04, CLI-01, UI-01]

must_haves:
  truths:
    - "All new test files exist and are discovered by pytest"
    - "New tests are marked xfail for missing implementation (collection passes, not errors)"
    - "Existing schema version tests updated to expect version 3 (not 2)"
    - "New migration test stubs exist for v2->v3"
  artifacts:
    - path: "tests/test_status.py"
      provides: "Test stubs for PROF-02, PROF-03, PROF-04 status panel rendering"
    - path: "tests/test_prompt.py"
      provides: "Test stubs for CLI-01, UI-01 prompt command output"
    - path: "tests/test_settings.py"
      provides: "Test stubs for CLI-01 settings --theme command"
    - path: "tests/test_themes.py"
      provides: "Test stubs for render/themes.py get_theme() correctness"
    - path: "tests/test_models.py"
      provides: "Updated schema version assertions (2->3)"
    - path: "tests/test_persistence.py"
      provides: "New v2->v3 migration test stub"
  key_links:
    - from: "tests/test_status.py"
      to: "src/devmon/commands/status.py"
      via: "CliRunner invocation"
      pattern: "CliRunner.*invoke"
    - from: "tests/test_persistence.py"
      to: "src/devmon/persistence/migrations.py"
      via: "_migrate_2_to_3 function"
      pattern: "_migrate_2_to_3"
---

<objective>
Create test scaffolds for Phase 3 before any implementation. All new tests start as xfail stubs. Existing model tests that assert schema_version == 2 are updated to assert == 3 (they will fail until Plan 02 bumps the schema, which is intentional — RED phase).

Purpose: Enforce Nyquist compliance — every task in Plans 02-04 has an automated verify command pointing at a pre-existing test file.
Output: 4 new test files + 2 updated test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-player-profile/03-CONTEXT.md
@.planning/phases/03-player-profile/03-RESEARCH.md

@src/devmon/models/state.py
@src/devmon/commands/status.py
@tests/test_models.py
@tests/test_persistence.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test scaffold files for new Phase 3 modules</name>
  <files>tests/test_status.py, tests/test_prompt.py, tests/test_settings.py, tests/test_themes.py</files>
  <read_first>
    - tests/conftest.py (understand fixtures available: tmp_devmon_home, game_state, etc.)
    - tests/test_models.py (understand existing test style — imports inside body not top-level)
    - src/devmon/commands/status.py (current status command to understand what CliRunner invokes)
    - src/devmon/models/state.py (PlayerProfile fields for test data construction)
    - .planning/phases/03-player-profile/03-RESEARCH.md (Pattern 6 for prompt output format, validation approach for Rich output)
  </read_first>
  <action>
Create four test files. All tests that depend on not-yet-built code are marked `@pytest.mark.xfail(strict=True, reason="Phase 3 not yet implemented")`.

**tests/test_themes.py** — tests for render/themes.py (module does not exist yet):
```python
import pytest

@pytest.mark.xfail(strict=True, reason="render/themes.py not yet implemented")
def test_get_theme_neon_returns_dict():
    from devmon.render.themes import get_theme
    t = get_theme("neon")
    assert isinstance(t, dict)
    assert "border" in t
    assert "xp_bar" in t
    assert "levelup_text" in t

@pytest.mark.xfail(strict=True, reason="render/themes.py not yet implemented")
def test_get_theme_classic_returns_dict():
    from devmon.render.themes import get_theme
    t = get_theme("classic")
    assert "border" in t
    assert "levelup_border" in t

@pytest.mark.xfail(strict=True, reason="render/themes.py not yet implemented")
def test_aliases_cyberpunk_and_rpg():
    from devmon.render.themes import get_theme
    neon = get_theme("neon")
    cyberpunk = get_theme("cyberpunk")
    assert neon == cyberpunk
    classic = get_theme("classic")
    rpg = get_theme("rpg")
    assert classic == rpg

@pytest.mark.xfail(strict=True, reason="render/themes.py not yet implemented")
def test_unknown_theme_falls_back_to_neon():
    from devmon.render.themes import get_theme
    result = get_theme("nonexistent")
    neon = get_theme("neon")
    assert result == neon
```

**tests/test_status.py** — tests for status command multi-panel + level-up banner:
```python
import pytest
from io import StringIO

@pytest.fixture
def runner():
    from typer.testing import CliRunner
    return CliRunner()

@pytest.mark.xfail(strict=True, reason="Multi-panel status not yet implemented")
def test_status_shows_level(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["status"])
    assert result.exit_code == 0
    assert "Level" in result.output

@pytest.mark.xfail(strict=True, reason="Multi-panel status not yet implemented")
def test_status_shows_xp_fraction(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["status"])
    assert result.exit_code == 0
    # XP fraction format: current/total (e.g., 0/100)
    import re
    assert re.search(r"\d+/\d+", result.output)

@pytest.mark.xfail(strict=True, reason="Multi-panel status not yet implemented")
def test_stats_panel_fields(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["status"])
    assert result.exit_code == 0
    # PROF-04: stats must show sessions, streak, battles, captures
    assert "Sessions" in result.output or "sessions" in result.output.lower()
    assert "Streak" in result.output or "streak" in result.output.lower()

@pytest.mark.xfail(strict=True, reason="Level-up banner not yet implemented")
def test_levelup_banner_clears_flag(tmp_devmon_home):
    """PROF-03: Banner renders when level_up_pending=True and clears the flag."""
    from devmon.models.state import GameState
    from devmon.persistence.save import save, load
    state = GameState.new_game("Trainer")
    state.player.level_up_pending = True
    state.player.pending_level_value = 2
    save(state)
    # Run devmon — startup should detect and clear the flag
    from typer.testing import CliRunner
    from devmon.main import app
    runner = CliRunner()
    result = runner.invoke(app, ["status"])
    assert result.exit_code == 0
    # Flag must be cleared in save
    reloaded = load()
    assert reloaded is not None
    assert reloaded.player.level_up_pending is False

@pytest.mark.xfail(strict=True, reason="Theme system not yet implemented")
def test_neon_theme_applied(runner, tmp_devmon_home):
    """PROF-02: Neon theme (default) renders without errors."""
    from devmon.main import app
    result = runner.invoke(app, ["status"])
    assert result.exit_code == 0

@pytest.mark.xfail(strict=True, reason="Level-up pending field not yet in model")
def test_level_up_pending_field_exists():
    """PROF-03: PlayerProfile has level_up_pending and pending_level_value fields."""
    from devmon.models.state import PlayerProfile
    p = PlayerProfile(name="Test")
    assert hasattr(p, "level_up_pending")
    assert p.level_up_pending is False
    assert hasattr(p, "pending_level_value")
    assert p.pending_level_value == 0
```

**tests/test_prompt.py** — tests for `devmon prompt` command (CLI-01, UI-01):
```python
import pytest

@pytest.fixture
def runner():
    from typer.testing import CliRunner
    return CliRunner()

@pytest.mark.xfail(strict=True, reason="devmon prompt command not yet implemented")
def test_prompt_exits_zero(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["prompt"])
    assert result.exit_code == 0

@pytest.mark.xfail(strict=True, reason="devmon prompt command not yet implemented")
def test_prompt_format_contains_level(runner, tmp_devmon_home):
    """UI-01: prompt output contains 'Lv.' prefix with level number."""
    from devmon.main import app
    result = runner.invoke(app, ["prompt"])
    assert "Lv." in result.output

@pytest.mark.xfail(strict=True, reason="devmon prompt command not yet implemented")
def test_prompt_format_contains_xp_fraction(runner, tmp_devmon_home):
    """UI-01: prompt output contains XP fraction like 0/100."""
    from devmon.main import app
    import re
    result = runner.invoke(app, ["prompt"])
    # Must match pattern: digits/digits
    assert re.search(r"\d+/\d+", result.output)

@pytest.mark.xfail(strict=True, reason="devmon prompt command not yet implemented")
def test_prompt_no_ansi_escape_codes(runner, tmp_devmon_home):
    """UI-01/D-07: No ANSI escape codes in prompt output (PS1-safe)."""
    from devmon.main import app
    import re
    result = runner.invoke(app, ["prompt"])
    # ANSI escape pattern: ESC[ sequences
    ansi_pattern = re.compile(r"\x1b\[[0-9;]*[mGKHF]")
    assert not ansi_pattern.search(result.output), "Prompt must not contain ANSI escapes"

@pytest.mark.xfail(strict=True, reason="devmon prompt command not yet implemented")
def test_prompt_no_save_returns_default(runner, tmp_devmon_home):
    """UI-01: prompt with no save file returns safe default string."""
    from devmon.main import app
    result = runner.invoke(app, ["prompt"])
    assert result.exit_code == 0
    assert len(result.output.strip()) > 0
```

**tests/test_settings.py** — tests for `devmon settings` command (CLI-01):
```python
import pytest

@pytest.fixture
def runner():
    from typer.testing import CliRunner
    return CliRunner()

@pytest.mark.xfail(strict=True, reason="devmon settings command not yet implemented")
def test_settings_shows_current_theme(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["settings"])
    assert result.exit_code == 0
    assert "Theme" in result.output or "theme" in result.output.lower()

@pytest.mark.xfail(strict=True, reason="devmon settings command not yet implemented")
def test_settings_theme_flag_sets_classic(runner, tmp_devmon_home):
    """CLI-01: --theme classic saves theme to config."""
    from devmon.main import app
    result = runner.invoke(app, ["settings", "--theme", "classic"])
    assert result.exit_code == 0
    # Config should be updated
    from devmon.config.loader import load_config
    import os
    os.environ["DEVMON_HOME"] = str(tmp_devmon_home)
    cfg = load_config()
    assert cfg["ui"]["theme"] == "classic"

@pytest.mark.xfail(strict=True, reason="devmon settings command not yet implemented")
def test_settings_invalid_theme_exits_nonzero(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["settings", "--theme", "invalid_theme_xyz"])
    assert result.exit_code != 0

@pytest.mark.xfail(strict=True, reason="devmon settings command not yet implemented")
def test_settings_theme_flag_sets_neon(runner, tmp_devmon_home):
    from devmon.main import app
    result = runner.invoke(app, ["settings", "--theme", "neon"])
    assert result.exit_code == 0
```

Do NOT import modules at the top level of test files — all `from devmon.*` imports must be inside test bodies (established convention from Phase 2).
  </action>
  <verify>
    <automated>cd C:/Users/flopp/OneDrive/Desktop/Devmon && uv run pytest tests/test_themes.py tests/test_status.py tests/test_prompt.py tests/test_settings.py --co -q 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` shows all 4 new test files without import errors
    - All tests in new files are collected (shown as xfail when run, not ERROR)
    - `uv run pytest tests/test_themes.py tests/test_status.py tests/test_prompt.py tests/test_settings.py -q` shows all tests as XFAIL (not ERROR or FAILED)
    - grep "xfail" tests/test_status.py returns matches on level_up, neon_theme, stats_panel tests
  </acceptance_criteria>
  <done>All 4 test files created, collected without import errors, all tests report XFAIL when run (strict=True xfail for unimplemented code)</done>
</task>

<task type="auto">
  <name>Task 2: Update existing tests for schema version 3 bump</name>
  <files>tests/test_models.py, tests/test_persistence.py</files>
  <read_first>
    - tests/test_models.py (read entire file — need to find all 4 schema_version == 2 assertions)
    - tests/test_persistence.py (find existing migration tests and add v2->v3 stub)
    - src/devmon/persistence/migrations.py (understand CURRENT_VERSION and migration dict structure)
  </read_first>
  <action>
**tests/test_models.py changes:**

Find and update these 4 tests (do not change anything else):
1. `test_gamestate_round_trip` — change `assert loaded.schema_version == 2` to `== 3`
2. `test_schema_version_present` — change `assert data["schema_version"] == 2` to `== 3`
3. `test_new_game_defaults` — change `assert state.schema_version == 2` to `== 3`
4. `test_schema_version_is_2` — rename to `test_schema_version_is_3`, change `assert state.schema_version == 2` to `== 3`

These will immediately fail (RED) because the model still defaults to 2. They go GREEN after Plan 02 bumps the schema. This is intentional.

**tests/test_persistence.py additions:**

Add one new test at the end of the file (xfail stub for v2->v3 migration):
```python
@pytest.mark.xfail(strict=True, reason="v2->v3 migration not yet implemented")
def test_migrate_v2_to_v3():
    """Schema migration v2->v3 adds level_up_pending=False and pending_level_value=0."""
    from devmon.persistence.migrations import migrate
    data = {
        "schema_version": 2,
        "player": {
            "name": "Trainer",
            "level": 1,
            "xp": 0,
        }
    }
    result = migrate(data)
    assert result["schema_version"] == 3
    assert result["player"]["level_up_pending"] is False
    assert result["player"]["pending_level_value"] == 0
```

Do not add this import at top level — use local import inside the function body following the established pattern (check conftest.py for the pattern used in Phase 2 tests).
  </action>
  <verify>
    <automated>cd C:/Users/flopp/OneDrive/Desktop/Devmon && uv run pytest tests/test_models.py tests/test_persistence.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "schema_version == 3" tests/test_models.py returns 4 matches (one per updated test)
    - grep "test_schema_version_is_3" tests/test_models.py returns 1 match
    - grep "test_migrate_v2_to_v3" tests/test_persistence.py returns 1 match
    - `uv run pytest tests/test_models.py -q` shows 4 FAILED tests (the updated schema version ones — correct RED state)
    - `uv run pytest tests/test_persistence.py -k test_migrate_v2_to_v3 -q` shows 1 XFAIL
    - All other tests in those files continue to pass (no regressions in non-schema tests)
  </acceptance_criteria>
  <done>4 schema version assertions updated to == 3. test_schema_version_is_2 renamed to test_schema_version_is_3. v2->v3 migration test stub added as xfail.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```
cd C:/Users/flopp/OneDrive/Desktop/Devmon && uv run pytest tests/ -q 2>&1 | tail -20
```

Expected state after Plan 01:
- 4 test_models.py tests FAIL (schema_version still 2, expected 3 — correct RED state)
- All new test files: tests collected as XFAIL (not ERROR)
- All other existing tests: PASS
- No import errors in any test file

This RED state is intentional. Plans 02-04 turn them GREEN.
</verification>

<success_criteria>
- All 4 new test files (test_themes.py, test_status.py, test_prompt.py, test_settings.py) exist and are collected without import errors
- All new tests show XFAIL (strict=True) when implementation does not exist yet
- test_models.py has 4 tests expecting schema_version == 3 (currently failing = RED = correct)
- test_persistence.py has test_migrate_v2_to_v3 as xfail stub
- `uv run pytest tests/ --co -q` shows 0 collection errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-player-profile/03-01-SUMMARY.md`
</output>
